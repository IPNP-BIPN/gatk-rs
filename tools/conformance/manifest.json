{
  "$comment": [
    "The conformance suites, declared once. `generate_ci.py` emits the oracle jobs of",
    ".github/workflows/ci.yml from this file, and `run_suite.py` runs the same steps locally.",
    "Same shape as picard-rs and htsjdk-rs; see picard-rs decision 0008 for why a golden that no",
    "CI step re-derives is not evidence."
  ],
  "oracle": {
    "image": "gatk-rs-oracle:4.6.2.0",
    "context": "tools/oracle",
    "platform": "linux/amd64"
  },
  "ci": {
    "suites_per_job": 5,
    "$why": "Suites share a job because each job restores the oracle image once."
  },
  "default_java_props": [
    "-Duser.language=en",
    "-Duser.country=US"
  ],
  "suites": [
    {
      "id": "annotation-original-alignment",
      "title": "OriginalAlignment and the OA tag",
      "status": "oracle-backed",
      "harness": "tools/annotation-conformance",
      "tools": [],
      "why": "OriginalAlignment counts the reads supporting the best alternate allele whose original alignment was on a different contig, which is how a Mutect2 call on the mitochondrion is checked against a NuMT, and four of its decisions live outside the class. The allele it counts for is picked by TLOD through getTumorLogOdds and maxElementIndex, so a tie goes to the earliest alternate and a TLOD of dot is not missing but -1, which after the log conversion is an ordinary number that can win the maximum. The contig comparison is a string field: getOAContig splits the OA tag on a comma and takes field zero, so an unmapped read, whose tag is star comma zero and so on, has original contig star, which differs from every real contig and therefore counts. The filter takes BestAllele.isInformative, which compares against the log10 threshold whatever base the matrix is in. And unlike Coverage, MappingQualityZero and CountNs, which treat null likelihoods as nothing to say, this one calls Utils.nonNull and throws.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "OriginalAlignmentDump",
          "golden": "crates/gatk-annotation/tests/data/original_alignment.txt.gz"
        }
      ]
    },
    {
      "id": "annotation-per-allele",
      "title": "MBQ, MMQ, MPOS and MFRL",
      "status": "oracle-backed",
      "harness": "tools/annotation-conformance",
      "tools": [],
      "why": "The four PerAlleleAnnotation members share one parent and disagree about everything the parent leaves open. An allele no read supports is annotated 0 by BaseQuality, 60 by MappingQuality, 50 by ReadPosition and 0 by FragmentLength, and the two invented values say why in a comment: an allele with no reads should not prejudice a site. Three filters run before a read contributes and they are not the same filter, so MappingQuality never sees a read of quality zero and its median can only be zero by way of the empty case, which is sixty. The value under the key is a Java int array, so it prints through Arrays.toString and carries the class [I rather than a list class. And includeRefAllele is false in the parent and overridden to true in three of the four, so MPOS reports one number per alternate allele where the others report one per allele including the reference. The median itself is commons-math3 Percentile finished with FastMath.round, which is floor(x + 0.5) and not Math.round; both are in jmath and this reaches the first.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "PerAlleleAnnotationDump",
          "golden": "crates/gatk-annotation/tests/data/per_allele.txt.gz",
          "why": "From the pinned container on a real x86-64 runner. The golden corrected the port on one point, and it is the shape of the data structure rather than the arithmetic: the buckets are keyed by the matrix's alleles while the loop runs over the variant's, so a variant allele the matrix never held looks up null and aggregate dereferences it. The port had it as an empty list, which would have produced the annotation's value for no reads, a plausible number where the reference produces no record at all."
        }
      ]
    },
    {
      "id": "variant-getters",
      "title": "An INFO attribute as a double array",
      "status": "oracle-backed",
      "harness": "tools/genotyper-conformance",
      "tools": [],
      "why": "Everything that reads a numeric annotation off a VariantContext goes through VariantContextGetters.attributeValueToDoubleArray, and the function is held together by an accident. Its Iterable branch tests value.getClass().isAssignableFrom(Iterable.class), which is backwards: isAssignableFrom asks whether the argument's type can be assigned to the receiver's, so it asks whether an Iterable fits in an ArrayList variable, which is false. Every list therefore falls through to the branch labelled as a last resort, which renders the value with String.valueOf and splits the result on a comma; String.valueOf of a list is bracketed and comma-space separated, so every field after the first arrives with a leading space and parses only because Double.parseDouble trims. A port splitting on comma-space, or using a parser that refuses leading whitespace, agrees on a one-element list and diverges on every longer one. Two more things the signature hides: a field equal to the missing value becomes the caller's missingValue, which is -1 for the overload every annotator uses, so a missing TLOD arrives as a number nobody can recognise as missing; and any other unparsable field becomes a GATKException naming the annotation rather than a NumberFormatException. The doubles travel as raw bits because getTumorLogOdds multiplies them by Math.log(10).",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "VariantGettersDump",
          "golden": "crates/gatk-engine/tests/data/variant_getters.txt.gz"
        }
      ]
    },
    {
      "id": "annotation-likelihood-counts",
      "title": "The annotations that read the likelihood matrix",
      "status": "oracle-backed",
      "harness": "tools/annotation-conformance",
      "tools": [],
      "why": "Coverage, MappingQualityZero and CountNs are the three annotations whose whole input is the likelihood matrix. Their arithmetic is three counts and no two of the three agree on anything else. Only Coverage tests evidenceCount, so an empty matrix makes it write nothing while the other two write a zero. Only MappingQualityZero tests the site, so a non-variant site makes it write nothing while the other two still count. Coverage and MappingQualityZero go through String.format so their values are Strings, while CountNs puts a long into an ImmutableMap so its value is a Long: all three render identically and are three different objects to a consumer. And CountNs compares against the byte N, upper case only, reaching the base through getReadBaseAtReferenceCoordinate, whose bounds test uses the alignment span while its index uses the soft start, so a read whose N sits inside a soft clip does not count.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "LikelihoodAnnotationDump",
          "golden": "crates/gatk-annotation/tests/data/likelihood_annotations.txt.gz"
        }
      ]
    },
    {
      "id": "genotyper-allele-likelihoods",
      "title": "The likelihood matrix and the best-allele search",
      "status": "oracle-backed",
      "harness": "tools/genotyper-conformance",
      "tools": [],
      "why": "Every likelihood-reading annotation goes through searchBestAllele, and four of its decisions are invisible in its signature. bestAlleleIndex and secondBestIndex both start at zero and nothing means no second best, so a one-allele matrix ends with the two equal and the tail turns that into a second-best likelihood of negative infinity. The comparison is strictly greater, so among equal likelihoods the earliest allele wins and the allele order is observable in a result that looks order-independent. isInformative compares the confidence against LOG_10_INFORMATIVE_THRESHOLD whatever base the matrix is in, while the tie-breaking pass inside searchBestAllele converts its threshold with the base, so after switchToNaturalLog the two ask their question at different thresholds. And confidence is guarded by likelihood == secondBestLikelihood ? 0 : the difference, whose load-bearing case is the pair of negative infinities where the subtraction would be NaN. The likelihoods travel as raw bits because they are read back out of the matrix after a conversion.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "AlleleLikelihoodsDump",
          "golden": "crates/gatk-engine/tests/data/allele_likelihoods.txt.gz"
        }
      ]
    },
    {
      "id": "genotyper-allele-list",
      "title": "The allele and sample axes",
      "status": "oracle-backed",
      "harness": "tools/genotyper-conformance",
      "tools": [],
      "why": "AlleleLikelihoods is defined over an AlleleList and a SampleList, so anything surprising about those two is inherited by every likelihood GATK computes. An indexed list is a set: IndexedSet drops a duplicate silently and the survivor keeps the index of its first occurrence, so a list built from A, C, A has two entries and nothing says so, and a caller that built its list from a variant context with a repeated allele gets a matrix with fewer rows than it asked for. Membership is Allele.equals, which is bases and the reference flag, so the reference A and a non-reference A are two entries in one list and indexOfReference reports only the first entry whose flag is set. And a permutation is a directional subset map: it may drop and reorder, never invent, refusing a longer target and a target holding an unknown allele with the same message both ways, while reporting isPartial, isNonPermuted and isKept, which callers use to skip work. A subset that keeps its order is partial and NOT non-permuted, which is the distinction a port collapses by accident.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "AlleleListDump",
          "golden": "crates/gatk-engine/tests/data/allele_list.txt.gz"
        }
      ]
    },
    {
      "id": "annotation-counting",
      "title": "The counting INFO annotations",
      "status": "oracle-backed",
      "harness": "tools/annotation-conformance",
      "tools": [],
      "why": "ChromosomeCounts, SampleList and RawGtCount are the members of the counting family that need no AlleleLikelihoods, so they can be asked through their own interface. What is measured is not only the numbers but the shape of what an annotation returns, which is where a port goes wrong silently. The map is Map<String,Object>, so the Java class of each value is part of the answer: ChromosomeCounts puts an Integer for one alternate allele and an ArrayList for two or more, and both render the same way. An empty map is not zeroes, it means the keys are absent from the record, and each annotation has at least one guard reaching that branch. RawGtCount.annotate returns null rather than an empty map, a third thing again, and its real work is combineRawData, whose output drops one of the three counts it just summed and writes a literal dot in its place, so the result of a combine cannot be fed back into one. And SampleList's guard is isMonomorphicInSamples, which is not every genotype is hom-ref: a site with alternate alleles and no genotypes at all is not monomorphic, while a site where everybody is a no-call is.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "AnnotationDump",
          "golden": "crates/gatk-annotation/tests/data/annotations.txt.gz"
        }
      ]
    },
    {
      "id": "readfilters",
      "title": "ReadFilterLibrary decisions",
      "status": "oracle-backed",
      "harness": "tools/readfilter-conformance",
      "tools": [],
      "why": "One row per filter, one character per record: the reference's own decision, taken through SAMRecordToGATKReadAdapter rather than off the flags. Going through the adapter is the point: GATKRead.isUnmapped is three criteria and isFirstOfPair requires pairing, so comparing against flags would compare against a reimplementation of the adapter instead of the reference. The corpus travels in the same file as SAM text, so the port judges the records the reference judged rather than a description of them.",
      "compare": {
        "mode": "lines"
      },
      "$compare_why": "Line mode, not keyed. Keyed compares one row per (kind, case) pair, and every dump here repeats a kind across many cases: the ReadClipper golden has 2,385 rows and only 181 distinct keys, so keyed mode silently compared 8% of it and reported ok. The whole file is the artefact and its order is part of it.",
      "cases": [
        {
          "dump": "ReadFilterDump",
          "golden": "crates/gatk-readfilter/tests/data/read_filters.txt.gz"
        },
        {
          "dump": "ReadCoordinateDump",
          "golden": "crates/gatk-engine/tests/data/read_coordinates.txt.gz",
          "why": "ReadUtils maps a reference position to a read offset, and every walker, annotation and clipping operation stands on that arithmetic. It is probed from three bases before each read to three past it, so the not-found answers at both edges are measured rather than assumed."
        },
        {
          "dump": "CigarClipDump",
          "golden": "crates/gatk-engine/tests/data/cigar_clipping.txt.gz",
          "why": "Every cigar ReadClipper produces goes through CigarUtils.clipCigar and CigarBuilder, and the builder rewrites what it is handed: it merges, drops zero-length elements, removes deletions that land at either end, and moves a deletion that arrives after an insertion in front of it. Each rewrite is a byte difference in the output BAM of every clipping tool."
        },
        {
          "dump": "ReadClipperDump",
          "golden": "crates/gatk-engine/tests/data/read_clipping.txt.gz",
          "why": "Clipping rewrites the read: bases, qualities, cigar, alignment start and, for a flow read group, some of its tags. The golden carries the whole clipped read for every entry point, because comparing one field would hide the others."
        },
        {
          "dump": "ReferenceQueryDump",
          "golden": "crates/gatk-engine/tests/data/reference_query.txt.gz",
          "why": "The bases a tool sees are not the bytes in the FASTA: CachingIndexedFastaSequenceFile upper-cases every query and flattens every IUPAC code to N. The fixture travels in the golden so the port reads the bytes the reference read, and the comparison covers the transformation rather than the file format."
        },
        {
          "dump": "CountingFilterDump",
          "golden": "crates/gatk-readfilter/tests/data/counting_filters.txt.gz",
          "why": "CountingReadFilter's summary text is output a tool prints, so it is compared as bytes. It is also the only place the order of a conjunction is observable: the boolean cannot say which of WellformedReadFilter's eight filters rejected a read, and the counts can."
        }
      ]
    },
    {
      "id": "readsdatasource",
      "title": "ReadsDataSource interval queries",
      "status": "oracle-backed",
      "harness": "tools/readfilter-conformance",
      "tools": [],
      "why": "Which reads a walker sees for a given -L is decided by htsjdk's stateful query filter, not by an overlap test: its interval index only advances and the traversal stops once every interval is behind the record, unmapped reads carrying their mate's position are special-cased, and QueryInterval.optimizeIntervals merges abutting intervals so two adjacent -L arguments return a spanning read once. The fixture BAM and its .bai travel in the golden as base64, so the port queries the bytes the reference queried.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "ReadsDataSourceDump",
          "golden": "crates/gatk-engine/tests/data/reads_query.txt.gz",
          "why": "Produced by the pinned container on a real x86-64 runner and downloaded from that run's artifact, which is the only provenance this programme accepts for a golden. Three of its answers are worth naming: a query at 500-500 does not return the mapped read that starts at 500 with an empty cigar, because its alignment end is 499; a query at 495-505 does; and 499-499 stops the traversal outright."
        }
      ]
    },
    {
      "id": "referencecontext",
      "title": "ReferenceContext windows",
      "status": "oracle-backed",
      "harness": "tools/readfilter-conformance",
      "tools": [],
      "why": "A walker never queries the reference directly: it is handed a window around its position, and three of that window's behaviours are decisions. Near a contig edge the window is silently smaller than requested and numWindowLeadingBases reports what was obtained rather than what was asked for; getBases(leading, trailing) expands the window rather than the interval, so the expansions compose; and getKmerAround returns null rather than a shorter kmer when the edge prevents expansion. Each accessor gets its own row, so which call throws is measured rather than collapsed into one E.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "ReferenceContextDump",
          "golden": "crates/gatk-engine/tests/data/reference_context.txt.gz",
          "why": "From the pinned container on a real x86-64 runner. Its sharpest row is the asymmetry between the two ways of asking for the same window: setWindow(3, 3) at chr1:1-1 crops silently to chr1:1-4, while the same window given as coordinates throws, because building SimpleInterval(chr1, -2, 4) is itself the error."
        }
      ]
    },
    {
      "id": "readwalker",
      "title": "ReadWalker traversal",
      "status": "oracle-backed",
      "expect_rows": 100,
      "expect_contains": [
        "deflaterafterdict\tcom.intel.gkl.compression.IntelDeflaterFactory",
        "deflater\thtsjdk.samtools.util.zip.DeflaterFactory"
      ],
      "harness": "tools/readfilter-conformance",
      "tools": [],
      "why": "Every read-based tool inherits this traversal, so which reads reach apply and in what order is the tool's output. The probe is a real ReadWalker run through the real command line rather than a reconstruction: it records, per apply call, the read and the ReferenceContext window it arrived with. Two decisions are invisible from any tool's own output: the filter sits between the two read transformers, so it judges a read the walker never sees; and getReadInterval is null for a mapped read whose coordinates are not a valid interval, which hands the walker a read with no reference under it.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "ReadWalkerDump",
          "golden": "crates/gatk-tools/tests/data/read_walker.txt.gz",
          "why": "From the pinned container on a real x86-64 runner. Refreshed to add the five -L unmapped runs: setTraversalBounds makes a traversal bounded when it has intervals or when unmapped reads were asked for, and loadNextIterator runs the interval query first and the unmapped query second, so the unplaced reads are a tail rather than an interleaving and -L unmapped alone is a bounded traversal of nothing else. It caught a divergence the class reading would not have: run without -R, a walker still receives a ReferenceContext whose *window* is the read's interval, with only its bases empty. The first version of the port handed over the fully empty context and would have reported a null window for every read of every reference-less run."
        }
      ]
    },
    {
      "id": "printreads",
      "title": "PrintReads output bytes",
      "status": "oracle-backed",
      "harness": "tools/readfilter-conformance",
      "tools": [
        "PrintReads"
      ],
      "why": "The first whole tool, and the first suite whose claim is bytes rather than decisions: the output BAM travels in the golden in full, base64, index included. Two header behaviours are measured with it: the @PG record the tool appends (ID, VN, CL, PN, with the ID taking a numeric suffix when it collides) and the fact that getHeaderForSAMWriter mutates the reads header in place. The dump installs htsjdk's JDK DeflaterFactory explicitly and records it in the golden, because --use-jdk-deflater does not restore it: GATK, like Picard, only ever installs the Intel factory when the flag is false, and that setter is static, so one earlier Picard call in the same JVM leaves GKL compressing everything that follows while the flag claims otherwise. The dump writes to a fixed directory rather than a temporary one, because the command line is recorded inside the output BAM's own @PG record: a random /tmp path does not make one golden row unstable, it makes every output byte unstable, and canonicalization cannot reach inside base64.",
      "compare": {
        "mode": "lines"
      },
      "expect_contains": [
        "commandline\tall\t",
        "index\tnoindex\tabsent",
        "deflater\thtsjdk.samtools.util.zip.DeflaterFactory",
        "@PG"
      ],
      "$expect_why": "The tool records a command line, the run that asked for no index produced none, the deflater that produced the bytes is the JDK one and says so in the golden, and the header carries the appended program record.",
      "cases": [
        {
          "dump": "PrintReadsDump",
          "golden": "crates/gatk-tools/tests/data/print_reads.txt.gz",
          "why": "From the pinned container on a real x86-64 runner. Six output BAMs and five indexes, byte for byte, with the deflater that produced them named in the file."
        }
      ]
    },
    {
      "id": "record-transform",
      "status": "oracle-backed",
      "title": "UnmarkDuplicates and RevertBaseQualityScores output bytes",
      "harness": "tools/readfilter-conformance",
      "tools": [
        "UnmarkDuplicates",
        "RevertBaseQualityScores"
      ],
      "why": "G2's calibration gate: the second and third whole tools, so the marginal cost of an archetype member can be measured rather than guessed once PrintReads has paid for the engine. One dump covers both, which is itself part of the answer. Three behaviours are the reason for the case list and none of them is the transform. Both tools override getDefaultReadFilters to ALLOW_ALL_READS where PrintReads takes GATKTool's default of WellformedReadFilter, so on the same file the three tools emit different sets of reads and it is the filter rather than the apply that decides it; the wellformed case asks for the filter explicitly so the difference is visible in the golden. RevertBaseQualityScores aborts the whole run on a read with no OQ rather than skipping it or passing it through, so the output is whatever had been flushed and a port that passed the read through would produce a larger and healthier-looking file than the reference. And an empty OQ is the same as an absent one, because getOriginalBaseQualities returns null for both even though fastqToPhred of an empty string is happy and returns an empty array; that conflation is the reference's and is measured rather than inferred. The output BAMs travel in full, base64, indexes included, and the deflater is pinned and named in the golden because the factory is static and whoever touches it first wins for the life of the JVM.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "RecordTransformDump",
          "golden": "crates/gatk-tools/tests/data/record_transform.txt.gz",
          "why": "From the pinned container on a real x86-64 runner, published as the golden-pending artefact of the run that added this suite. 49 rows, no corrections, and byte-identical to the same container on Apple Silicon. Twelve output BAMs and eleven indexes byte for byte, three input fixtures with their indexes, and the two runs that abort with the class and message the reference throws."
        }
      ]
    },
    {
      "id": "fix-misencoded",
      "status": "oracle-backed",
      "title": "FixMisencodedBaseQualityReads, the fourth record-transform tool",
      "harness": "tools/readfilter-conformance",
      "tools": [
        "FixMisencodedBaseQualityReads"
      ],
      "why": "The fourth whole tool of the largest archetype, and the first whose transform can REFUSE THE FILE IT WAS GIVEN. Its apply is one line; everything worth measuring is elsewhere. IT DOES NOT OVERRIDE ITS READ FILTERS, so it takes GATKTool's default of WellformedReadFilter where UnmarkDuplicates and RevertBaseQualityScores replace the whole list with ALLOW_ALL_READS: three tools in one archetype and two different default traversals, which is why naming AllowAllReadsReadFilter changes what this one emits and would not change what the other two emit. A QUALITY BELOW 31 ABORTS THE WHOLE RUN with a message saying the read 'was correctly encoded', so a port that clamped would write a healthy-looking file the reference never wrote. THE REFUSAL IS A PROPERTY OF THE TRAVERSAL RATHER THAN OF THE FILE: the same fixture succeeds when an interval excludes the read that carries the low quality, which the corpus carries as two cases over one file. THE CHECK IS PER BASE AND AFTER THE SUBTRACTION, so the bases before the offending one are already rewritten when it throws. AND A READ WITH NO QUALITIES AT ALL PASSES THROUGH, because the loop never runs: '*' in a SAM is not a quality of zero.",
      "compare": {
        "mode": "lines"
      },
      "expect_rows": 40,
      "cases": [
        {
          "dump": "FixMisencodedDump",
          "golden": "crates/gatk-tools/tests/data/fix_misencoded.txt.gz",
          "why": "40 rows: three fixtures with their indexes, eight runs whose output BAM and index travel in full, and one refusal with the class and message the reference throws. The fixtures differ only in their qualities, which is what every case turns on. From the pinned container on a real x86-64 runner, published as the golden-pending artefact of run 31230160751, and byte for byte what an Apple Silicon laptop produced, the output BAMs included."
        }
      ]
    },
    {
      "id": "add-oa-tags",
      "status": "oracle-backed",
      "title": "AddOriginalAlignmentTags, which aborts on an unpaired read",
      "harness": "tools/readfilter-conformance",
      "tools": [
        "AddOriginalAlignmentTags"
      ],
      "why": "The fifth whole tool of the record-transform archetype and the first that writes tags rather than changing the read. IT ABORTS ON AN UNPAIRED READ: addMateContigTag calls mateIsUnmapped, which htsjdk defines only for a PAIRED read, so an unpaired one throws IllegalStateException and the whole run stops. Three of the four measured runs abort on a fixture holding one unpaired read among six, and the one that succeeds does so because its interval excludes it. An unpaired read is ordinary in an ordinary file, so this is the tool's behaviour on ordinary input rather than an edge case. THE OA FORMAT'S COMMA ESCAPING CANNOT FIRE: it replaces a comma in the contig name with an underscore, and a comma is not a legal sequence name character at all, which the golden carries as htsjdk's refusal and the regex it quotes. A READ WITH NO NM STILL GETS AN OA and the missing field prints as the four characters 'null', because getAttributeAsString returns null into a format string. AN UNMAPPED MATE GIVES XM=* and an unmapped read the constant '*,0,*,*,0,0;'. The tags are read back off the output rather than inferred, and the reads that survive an aborted run are in the golden too: the writer had already flushed five of six.",
      "compare": {
        "mode": "lines"
      },
      "expect_rows": 16,
      "cases": [
        {
          "dump": "AddOriginalAlignmentTagsDump",
          "golden": "crates/gatk-tools/tests/data/add_original_alignment_tags.txt.gz",
          "why": "16 rows: one fixture with its index, four runs of which three abort, the one output BAM and its index in full, five tag rows read back off that output, and the sequence-name refusal that makes the comma branch dead. From the pinned container on a real x86-64 runner, published as the golden-pending artefact of run 31254191005, and byte for byte what an Apple Silicon laptop produced."
        }
      ]
    },
    {
      "id": "print-distant-mates",
      "status": "oracle-backed",
      "title": "PrintDistantMates, which moves a read to its mate and sorts the result",
      "harness": "tools/readfilter-conformance",
      "tools": [
        "PrintDistantMates"
      ],
      "why": "The sixth whole tool of the record-transform archetype, and three things in it are new to the archetype. IT EXTENDS THE DEFAULT READ FILTERS rather than taking or replacing them: getDefaultReadFilters starts from super's WellformedReadFilter and adds PAIRED, PRIMARY_LINE, NOT_DUPLICATE and MATE_DISTANT, a third pattern beside PrintReads which takes the default and UnmarkDuplicates which replaces it. Both lists are dumped rather than described. IT WRITES THE OA TAG WITH A DIFFERENT CONVENTION FROM AddOriginalAlignmentTags: both build 'contig,start,strand,cigar,mapq,NM;' and a missing NM prints as EMPTY here where the other tool prints the four characters 'null'. Both tools run over the same fixture so the two spellings of one missing field land in the same golden. IT OPENS ITS WRITER WITH preSorted = false where every other tool of the archetype passes true, and the transform moves each read into its mate's position, so traversal order and output order disagree by construction: the fixture's three surviving reads leave the traversal for chr2:600, chr1:2500 and chr2:150. undoDistantMateAlterations is measured as a round trip against the original record's own SAM text, tag block included, and on four OA values it cannot use.",
      "compare": {
        "mode": "lines"
      },
      "expect_rows": 75,
      "expect_contains": [
        "filters\tPrintDistantMates\t4\tMateDistantReadFilter",
        "roundtrip\t"
      ],
      "cases": [
        {
          "dump": "PrintDistantMatesDump",
          "golden": "crates/gatk-tools/tests/data/print_distant_mates.txt.gz",
          "why": "75 rows: the two filter lists, one fixture with its index, six runs with their headers, command lines, output BAMs and indexes in full, the reads read back off every output in the order they were written, the round trip over every fixture read, and undo's refusals. From the pinned container on a real x86-64 runner, published as the golden-pending artefact of run 31512787457, and byte for byte what an Apple Silicon laptop produced."
        }
      ]
    },
    {
      "id": "split-reads",
      "status": "oracle-backed",
      "title": "SplitReads, whose outputs come from the header rather than the reads",
      "harness": "tools/readfilter-conformance",
      "tools": [
        "SplitReads"
      ],
      "why": "The seventh whole tool of the record-transform archetype, and the first whose -O is a DIRECTORY and whose run opens more than one writer. THE SET OF OUTPUT FILES COMES FROM THE HEADER: createWriters takes the cross product of each splitter's getSplitsBy(header), so a read group no read belongs to still gets a valid empty BAM. THE CROSS PRODUCT DOES NOT DEDUPLICATE: two read groups with the same sample give the same key twice, prepareSAMFileWriter runs twice on one path, and the first writer is never closed because closeTool only closes what the map still holds. A NULL VALUE IS SPELLED TWO WAYS: a read group with no LB gives the key '.null' from the header, because addKey concatenates the Object, and '.unknown' from a read, because getKey substitutes UNKNOWN_OUT_PREFIX, so the file the header promises is not the file the reads go to and both exist. A READ WITH NO READ GROUP cannot answer any splitter, and whether that aborts the run is decided by a filter rather than by the tool: WellformedReadFilter's HAS_READ_GROUP drops it first, so both runs are measured, one with the default filters and one with that filter disabled. WITH NO --split-* the key is the empty string and one file is written, named after the input. AND EVERY FILE OF A RUN HAS A DIFFERENT HEADER: getHeaderForSAMWriter ADDS a @PG record to the reads header in place and returns the same object, and a writer serialises the header when it is created, so the nth writer's file carries n records for this one tool, with ids suffixed .1, .2 and so on. The on-demand unknown writer, created during the traversal, carries the most.",
      "compare": {
        "mode": "lines"
      },
      "expect_rows": 77,
      "expect_contains": [
        "outfile\tlibrary\tplain.null.bam",
        "outfile\tlibrary\tplain.unknown.bam",
        "Unrecognized attribute value found: .s2.rg3.unknown",
        "error\tnorg-nofilter\tjava.lang.NullPointerException",
        "programs\tlibrary\tplain.unknown.bam\tupstream;GATK SplitReads;GATK SplitReads.1;GATK SplitReads.2;GATK SplitReads.3"
      ],
      "cases": [
        {
          "dump": "SplitReadsDump",
          "golden": "crates/gatk-tools/tests/data/split_reads.txt.gz",
          "why": "77 rows: two fixtures with their indexes, ten runs of which three abort, and every file each finishing run left in its output directory in full, indexes and @PG lists included, because which files exist is the measurement. From the pinned container on a real x86-64 runner, published as the golden-pending artefact of run 31514597878, and byte for byte what an Apple Silicon laptop produced twice."
        }
      ]
    },
    {
      "id": "left-align-indels",
      "status": "oracle-backed",
      "title": "AlignmentUtils.leftAlignIndels, and the shift that can go right",
      "harness": "tools/readfilter-conformance",
      "tools": [],
      "why": "The call LeftAlignIndels is built around, measured on its own because its interface is exactly measurable and needs no file: a cigar, a reference, a read and a start in; a cigar and two counts of removed deletion bases out. THE CIGAR IS WALKED RIGHT TO LEFT, accumulating an indel's ranges and resolving them only at an alignment block or the start of the cigar, so two indels with too few matching bases between them COLLIDE AND MERGE, which is a behaviour of the loop rather than of a named rule. normalizeAlleles CAN SHIFT RIGHT: it trims shared bases off the end and then off the front, and the front trim does startShift--, so the returned start shift is NEGATIVE on non-parsimonious input and leftAlignIndels reads that as newMatchOnLeftDueToTrimming. A port that typed the shift as unsigned would be wrong in a way no simple case shows, so the inner function is dumped on its own inputs with the ranges before and after, because it adjusts them as a side effect. A DELETION THAT REACHES THE START OF THE CIGAR IS DROPPED rather than emitted and the reference bases it removed are reported instead, which is what makes the tool move the read. TWO VALIDATIONS REFUSE RATHER THAN CLAMP, both IllegalArgumentException out of a util rather than a UserException out of a tool.",
      "compare": {
        "mode": "lines"
      },
      "expect_rows": 21,
      "expect_contains": [
        "leftalign\tdeletion-to-the-start",
        "leftalign\tinsertion-then-deletion\t3M2I2M2D3M",
        "normalize\tshifts-right"
      ],
      "cases": [
        {
          "dump": "LeftAlignIndelsDump",
          "golden": "crates/gatk-engine/tests/data/left_align_indels.txt.gz",
          "why": "A table of cigars over designed repeat structures and a table of allele ranges: homopolymers, a dinucleotide repeat, indels that cannot move, soft and hard clips the shift may not cross, two indels that collide, a deletion that walks off the start, both refusals, and the non-parsimonious ranges that shift right. From the pinned container on a real x86-64 runner, published as the golden-pending artefact of run 31516507658, and byte for byte what an Apple Silicon laptop produced."
        }
      ]
    },
    {
      "id": "left-align-indels-tool",
      "status": "oracle-backed",
      "title": "LeftAlignIndels, the first tool of the archetype that needs the reference",
      "harness": "tools/readfilter-conformance",
      "tools": [
        "LeftAlignIndels"
      ],
      "why": "The eighth whole tool of the record-transform archetype and the FIRST WHOSE requiresReference() IS TRUE. THE REFERENCE A READ WALKER SEES IS THE READ'S OWN SPAN, no padding, so left-alignment is bounded by the read's own window rather than by the contig; the window handed to each read is dumped beside the read so a port cannot quietly widen it. A LEFT-ALIGNED DELETION CAN MOVE THE READ: CigarBuilder drops a deletion that ends up leading and reports the reference bases it removed, and the tool moves the read right by that many, which makes this the second tool of the archetype that changes a read's position after PrintDistantMates and for an unrelated reason. TWO CLASSES OF READ ARE PASSED THROUGH UNTOUCHED before the call rather than by it, an unmapped read and a read whose cigar has one element or none, and the fixture carries both. A RUN WITH NO -R IS REFUSED by Barclay rather than by the engine's requiresReference, so the message is an argument-parsing one. AND A PICARD CALL IN THE SAME JVM REPLACES THE DEFLATER FACTORY: this dump needs a .dict, so it calls CreateSequenceDictionary, which installs IntelDeflaterFactory into the static BlockCompressedOutputStream default and makes every BAM written afterwards carry GKL bytes whatever --use-jdk-deflater says. The dump records what the call left behind and pins the factory again before any run; #160 has the audit of the other dumps that do not. The call itself is measured by the left-align-indels suite.",
      "compare": {
        "mode": "lines"
      },
      "expect_rows": 40,
      "expect_contains": [
        "refwindow\tr0\tchr1\t6\t15\tAAAAATTTTT",
        "deflaterafterdict\t",
        "reads\tall\tr0\t0\tchr1\t7\t9M",
        "error\tnoreference"
      ],
      "cases": [
        {
          "dump": "LeftAlignIndelsToolDump",
          "golden": "crates/gatk-tools/tests/data/left_align_indels_tool.txt.gz",
          "why": "The reference and its index, one fixture with its index, the reference window handed to every read, five runs of which one is refused, the output BAMs and their indexes in full, and every read read back off two of them. From the pinned container on a real x86-64 runner, published as the golden-pending artefact of run 31519030504, and byte for byte what an Apple Silicon laptop produced."
        }
      ]
    },
    {
      "id": "clip-reads",
      "status": "oracle-backed",
      "title": "ClipReads, whose three clippers read the read in machine-cycle order",
      "harness": "tools/readfilter-conformance",
      "tools": [
        "ClipReads"
      ],
      "why": "The ninth whole tool of the record-transform archetype and the FIRST THAT WRITES A SECOND OUTPUT THAT IS NOT A BAM. THE QUALITY CLIPPER READS THE READ IN MACHINE-CYCLE ORDER, NOT IN REFERENCE ORDER: it walks i from the last base to the first but indexes with `isReverseStrand() ? readLen - i - 1 : i`, so a reverse-strand read is scanned from its front and the clip it emits is 0..clipPoint rather than clipPoint..readLen-1. The fixture's two quality reads carry the bad qualities at opposite ends of the array, so a port that walked the array backwards on both would find no clip point at all on the reverse one. THE REPRESENTATION DECIDES WHETHER THE WRITER SORTS: presorted is true only for WRITE_NS, WRITE_NS_Q0S and WRITE_Q0S, and the other three get a sorting writer, which the fixture makes observable twice over, by a soft clip that moves a read forward and by a revert that moves another backwards past it. HARDCLIP_BASES AND REVERT_SOFTCLIPPED_BASES REVERT BEFORE THEY CLIP, in apply, before the ReadClipper is constructed, so every clipper sees a read whose cigar and start have already changed. THE SEQUENCE CLIPPER IS A java.util.regex MATCH, case-insensitive, reverse-complemented for a reverse-strand read, looping until find() fails so one sequence can clip a read twice; its counts are keyed by the argument string in a TreeMap, which prints an upper-case argument before a lower-case one. THE CYCLE CLIPPER IS 1-BASED AND INCLUSIVE, clamps a stop past the end of the read and drops a range whose start is past it. THE ADAPTER CLIPPER DOES NOT FLIP FOR STRAND, and is the one place the tool adds a tag rather than removing bases. AND --read DROPS EVERY READ IT DOES NOT NAME rather than passing it through, so a --read that names nothing produces an empty BAM and a statistics file whose percentages are NaN, which is what String.format(\"%.2f\", 0.0/0) prints in Java.",
      "compare": {
        "mode": "lines"
      },
      "expect_rows": 170,
      "expect_contains": [
        "reads\tqt\tr0\t0\tchr1\t1\t10M\tACGTANNNNN",
        "reads\tqt\tr1\t16\tchr1\t5\t10M\tNNNNNACGTA",
        "reads\thard\tr2\t0\tchr1\t3\t10M",
        "reads\tadapter\tr3\t16\tchr1\t15\t10M\tNNGTACGNNN\tIIIIIIIIII\tXF=3;RG=rg1;XT=8;tf=A;tm=A"
      ],
      "cases": [
        {
          "dump": "ClipReadsDump",
          "golden": "crates/gatk-tools/tests/data/clip_reads.txt.gz",
          "why": "One fixture with its index, fifteen runs covering the three clippers alone and together, all six clipping representations, the adapter tags, the --read filter that names nothing, the length filter and a run with no clipping argument at all. Each run travels as its output BAM and index in full, its header and @PG command line, its statistics file as escaped text, and every read read back off it. From the pinned container on a real x86-64 runner, published as the golden-pending artefact of run 31525426341, and byte for byte what an Apple Silicon laptop produced."
        }
      ]
    },
    {
      "id": "transfer-read-tags",
      "status": "oracle-backed",
      "title": "TransferReadTags, the first tool of the archetype that is not a walker",
      "harness": "tools/readfilter-conformance",
      "tools": [
        "TransferReadTags"
      ],
      "why": "The tenth whole tool of the record-transform archetype and the FIRST THAT IS NOT A WALKER: it extends GATKTool and overrides traverse() itself, so there is no filter chain, no read transformer and no interval bound, and a port that reached for the read walker would apply WellformedReadFilter and drop reads this tool writes. The fixture carries a read with no read group for exactly that reason. It is also the first with TWO read inputs, walked in lockstep by query name with ReadQueryNameComparator, which duplicates SAMRecordQueryNameComparator. EVERY TAG IS TRANSFERRED AS A STRING, whatever it was: getAttributeAsString then setAttribute(name, String), so XI:i:42 arrives as XI:Z:42 and XN:f:1.5 as XN:Z:1.5, and the type is lost the same way every time. AN ALIGNED READ PAST THE END OF THE UNMAPPED FILE IS SILENTLY DROPPED, because the catch-up loop is `while (unmappedSamIterator.hasNext())` and when it runs out the loop simply ends: no exception, no warning, one fewer read in the output. THE WRITER IS NOT TOLD THE READS ARE SORTED, so htsjdk sorts a queryname header with SAMRecordQueryNameComparator and its six tie-breaks after the name; the fixture carries a name whose two records sit in the file in the order that comparator swaps. NO OUTPUT CARRIES AN INDEX whatever --create-output-bam-index says, because the output is queryname sorted. And the refusals come from four different layers: Barclay for a missing --read-tags, before the tool is built at all; Utils.validate for a coordinate-sorted -I, which is the only sort order checked and only on the aligned side; a UserException for an empty unmapped file beside a non-empty aligned one; IllegalStateException for an aligned read that is not in the unmapped file, from either of two throw sites with the same message; and IllegalArgumentException out of Utils.nonNull for a matched read missing the tag, naming the unmapped read rather than the aligned one.",
      "compare": {
        "mode": "lines"
      },
      "expect_rows": 65,
      "expect_contains": [
        "reads\talltypes\ta1\t0\tchr1\t1\tRG=rg1:String;XI=42:String;XN=1.5:String;RX=AAA-CCC:String",
        "reads\tin:unmapped\ta1\t4\t*\t0\tRG=rg1:String;XI=42:Integer;XN=1.5:Float;RX=AAA-CCC:String",
        "reads\tunsorted\ta1\t65\tchr1\t1\tRX=AAA-CCC:String",
        "error\tcoordinate\tjava.lang.IllegalStateException:aligned sam must be sorted by queryname",
        "index\trx\tabsent"
      ],
      "cases": [
        {
          "dump": "TransferReadTagsDump",
          "golden": "crates/gatk-tools/tests/data/transfer_read_tags.txt.gz",
          "why": "Ten fixtures with no index between them, twelve runs of which six are refused, the output BAMs in full, the header and @PG command line of each, and every read read back off both the inputs and the outputs with the Java class of every tag value beside it, because the finding is a type change that the characters do not show. From the pinned container on a real x86-64 runner, published as the golden-pending artefact of run 31526755049, and byte for byte what an Apple Silicon laptop produced."
        }
      ]
    },
    {
      "id": "intervalwalker",
      "title": "IntervalWalker traversal",
      "status": "oracle-backed",
      "expect_rows": 79,
      "expect_contains": [
        "summary\tintersection-empty\tE",
        "summary\texclude-everything\tE",
        "apply\texclude-middle\t1\t"
      ],
      "harness": "tools/readfilter-conformance",
      "tools": [],
      "why": "IntervalWalker.traverse is five lines over userIntervals, so the traversal is the list and the list is IntervalArgumentCollection: -L, -XL, --interval-padding, --interval-exclusion-padding, --interval-set-rule and --interval-merging-rule. The probe runs a real IntervalWalker through the real command line for 24 combinations of those, and records what reached apply. The decisions it pins are not in the argument names: padding is clamped to the contig rather than extended past it, getIntervalsWithFlanks merges padded intervals with ALL whatever --interval-merging-rule says, INTERSECTION is a running fold whose empty accumulator makes a single -L the identity, an empty intersection is an error rather than an empty traversal, -XL with no -L subtracts from the whole reference, and -XL inside an interval splits it in two.",
      "compare": {
        "mode": "lines"
      },
      "$expect_why": "The two failing set operations must fail, and the split that -XL performs in the middle of an interval must produce a second apply call.",
      "cases": [
        {
          "dump": "IntervalWalkerDump",
          "golden": "crates/gatk-tools/tests/data/interval_walker.txt.gz",
          "why": "From the pinned container on a real x86-64 runner. Two of its rows were not what reading the classes predicted: -XL chr1 with no -L is a MissingArgument, because requiresIntervals() makes -L a required Barclay argument and the whole-reference branch of parseIntervals is unreachable from this walker; and padding two arguments that come to overlap merges them even under OVERLAPPING_ONLY, because getIntervalsWithFlanks hard-codes ALL."
        }
      ]
    },
    {
      "id": "alignmentstate",
      "title": "AlignmentStateMachine stops",
      "status": "oracle-backed",
      "harness": "tools/readfilter-conformance",
      "tools": [],
      "why": "LocusIteratorByState runs one AlignmentStateMachine per read and merges their positions into a pileup, so every depth, allele count and pileup annotation rests on the offsets this produces. The dump records one row per stepForwardOnGenome call over 26 cigars chosen for the machine's corners: a deletion returns once per reference base it spans with the read offset frozen, I/S/H/P are consumed whole inside one step so no stop ever lands on them, a read ending on an insertion advances the genome offset one past its end anyway, and a cigar starting or ending with a deletion throws while a zero-length element is skipped in silence.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "AlignmentStateDump",
          "golden": "crates/gatk-engine/tests/data/alignment_state.txt.gz",
          "why": "From the pinned container on a real x86-64 runner: 244 stops over 26 cigars, 2 of them refused. Its sharpest rows are the ends: 5M3I stops 6 times for a read 8 bases long, because the trailing insertion is consumed whole and the final step advances the genome offset one past the read anyway; and 5M3D throws where 5M0D5M does not, so a deletion at the end is malformed while a deletion of nothing is skipped."
        }
      ]
    },
    {
      "id": "pileupelement",
      "title": "PileupElement answers",
      "status": "oracle-backed",
      "harness": "tools/readfilter-conformance",
      "tools": [],
      "why": "AlignmentStateMachine decides where a read stops; PileupElement decides what a caller sees there, and that is where the indel-aware predicates live. Two families of them disagree on purpose: isBeforeInsertion goes through getAdjacentOperator, which looks at exactly the next cigar element whatever it is, while isBeforeDeletionStart goes through getNearestOnGenomeCigarElement, which skips everything that is not M, =, X or D. On 3M2S3I3M the element before the clip is therefore not before an insertion, while a deletion behind a clip is still seen. A port using one walk for both would agree on every simple cigar and diverge exactly there. The dump also probes createPileupForReadAndOffset at every read offset, including the ones inside a soft clip or an insertion where the reference throws, and sets BI/BD on three reads so the flat Q45 fallback is measured against a real array.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "PileupElementDump",
          "golden": "crates/gatk-engine/tests/data/pileup_element.txt.gz",
          "why": "From the pinned container on a real x86-64 runner: 217 elements over 25 cigars, plus 231 createPileupForReadAndOffset calls. Its decisive rows are on 3M2S3I3M, where the last base of the leading 3M reports isBeforeSoftClip and not isBeforeInsertion while carrying 2S3I between it and the next position, and where the first base after the clip reports isAfterInsertion and not isAfterSoftClip. The two answers come from two different walks over the same cigar."
        }
      ]
    },
    {
      "id": "intervalfile",
      "title": "What one -L argument resolves to",
      "status": "oracle-backed",
      "harness": "tools/readfilter-conformance",
      "tools": [],
      "why": "IntervalWalkerDump measures what happens once the intervals exist; this measures the step before it. IntervalUtils.parseIntervalArguments decides whether a -L string is a Feature file, a GATK interval file, a file it refuses, or a literal interval, and the order of those tests is the behaviour. A Feature file is recognised first by asking every codec, but the codecs answer canDecode by extension rather than by content, which the golden settled: a .list holding a BED body is NOT a Feature file, falls through to the interval-file reader, and dies parsing chr1<tab>0<tab>10 as a genome location. The interval-file test is by lower-cased extension only and throws when the extension matches but the file is absent, because a contig name may contain a period and the contents cannot be consulted; an existing file that is neither is an error naming both; and only a non-existent argument with neither extension is parsed as a literal, which is why a typo in a filename surfaces as a missing contig. .interval_list and .bed are in the probe because they are Feature files rather than interval files, which their names do not suggest. The interval list adds four rows of its own, because it is the one format here validated against two dictionaries: the file's own @SQ lines, which the codec reads before the first record, and the reference dictionary that createGenomeLoc checks the result against. A contig the file does not declare is dropped and the file still loads, while a contig the file declares and the reference does not kills the argument, and featureFileToIntervals catches IllegalArgumentException and nothing else, so a bad strand and a wrong field count in the same file raise two different exception classes. VCF is the third codec and the only one that decides by content: canDecode reads the first eighteen bytes and looks for the magic, so the same body is a Feature file under any extension, which is the counterpart of the .list-holding-BED row that is not. Its rows also settle three refusals that are three different classes: a malformed record leaves the engine as a TribbleException, a header the codec refuses never yields a reader and surfaces as a GATKException, and a contig the reference dictionary does not hold is a MalformedGenomeLoc.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "IntervalFileDump",
          "golden": "crates/gatk-engine/tests/data/interval_file.txt.gz",
          "why": "From the pinned container on a real x86-64 runner. All 25 rows compared, no pending case. The golden corrected the port on three points: a one-base locus prints as contig:start with no range, since GenomeLoc.toString drops the range when start equals stop; a malformed VCF record is a TribbleException rather than an unreadable file; and a malformed VCF header is a GATKException, which is neither."
        }
      ]
    },
    {
      "id": "jexlfilter",
      "title": "JexlExpressionReadTagValueFilter decisions",
      "status": "oracle-backed",
      "harness": "tools/readfilter-conformance",
      "tools": [],
      "why": "The 56th read filter, and the only one that runs an expression language. Its answers are commons-jexl 2.1.1's, reached through one detail of the filter: the context is read.getAttributeAsString(name), so every attribute arrives as a String whatever it is in the BAM, and the *literal* decides which branch of JexlArithmetic.compare runs. NM > 3 is integral because 3 is an Integer and isNumberable is true; NM > 3.5 is floating because ASTNumberLiteral.setReal defaults to Float rather than Double; NM > '3' is lexicographic, so \"30\" > \"3\" is true for a different reason than 30 > 3 is. Only + falls back to concatenation, and only from a catch of NumberFormatException, so NM + 1 on a non-numeric tag is a string while NM - 1 is an error. The filter's own test is !v.equals(Boolean.TRUE), which makes a null result a NullPointerException rather than a false. The dump records the engine's value and the filter's boolean separately, plus the string the context handed over for every tag, so a divergence lands on the coercion rather than on the rendering.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "JexlFilterDump",
          "golden": "crates/gatk-readfilter/tests/data/jexl_filter.txt.gz",
          "why": "From the pinned container on a real x86-64 runner: 186 evaluations and 186 filter decisions over 6 reads. It corrected the port on the sharpest point: an absent tag does not evaluate to null. Interpreter.visit(ASTIdentifier) throws JexlException.Variable when the value is null and context.has(name) is false, and setLenient(false) makes the engine strict, so even ZZ == null throws rather than answering true. An absent tag can never be tested for."
        }
      ]
    },
    {
      "id": "featurecache",
      "title": "FeatureDataSource lookahead cache",
      "status": "oracle-backed",
      "harness": "tools/readfilter-conformance",
      "tools": [],
      "why": "What a tool sees when it asks which features are at a position is not what the file holds there: it is what the lookahead cache kept. Two behaviours are observable rather than internal. refillQueryCache extends the query end by queryLookaheadBases and caches everything the reader returned unfiltered, and getCachedFeaturesUpToStopPosition tests only start > stopPosition and *breaks*, so a feature that ends before the query started is still returned and one feature starting past the stop hides every feature behind it. trimToNewStartPosition pops the features starting before the new start, keeps those still overlapping, and pushes them back in reverse, preserving the file order, so a port that re-sorted would agree on every set and differ on every list. cacheHit is containment rather than overlap, so a query one base past the cached end is a miss. The lookahead is probed at 0 as well, because IntervalWalker.initializeFeatures sets it to 0 on purpose. What the golden then showed is worth stating plainly: the twenty answers are identical at lookahead 0 and at 100. The cache is transparent for well-ordered access, because trimToNewStartPosition keeps exactly the features a fresh overlap query would have returned. So this suite pins what a tool sees, which is the thing that matters, and it does NOT distinguish the cache from a fresh query per call: a port with no cache at all would pass it. The cache is ported for the ordering guarantee rather than because the suite forces it, and a probe that would separate them needs a feature file whose records are out of start order, which Tribble refuses to index.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "FeatureCacheDump",
          "golden": "crates/gatk-engine/tests/data/feature_cache.txt.gz"
        }
      ]
    },
    {
      "id": "readpileup",
      "title": "ReadPileup answers",
      "status": "oracle-backed",
      "harness": "tools/readfilter-conformance",
      "tools": [],
      "why": "What one locus answers, which is what a caller consults. Four of those answers are decisions rather than aggregation. getBaseCounts counts the wildcard * as an A, because BaseUtils.baseIndexMap maps it to Base.A.ordinal() with a comment saying so, while an N is dropped by the -1 check and a deletion by an explicit test: two exclusions with two different reasons. splitBySample throws when a read has no sample and unknownSampleName is null, and a read group that exists but declares no SM is indistinguishable downstream from a read with no group at all. sortedIterator sorts by the read's start and by nothing else, and Java's sort is stable, so ties keep assembly order. fixPairOverlappingQualities truncates twice and differently: agreeing bases sum into a byte and are then capped, so the cap tests for a negative stored value as well as for one over 93, while disagreeing bases multiply the winner by 0.8 in double arithmetic and cast back.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "ReadPileupDump",
          "golden": "crates/gatk-engine/tests/data/read_pileup.txt.gz",
          "why": "From the pinned container on a real x86-64 runner: 3 pileups and 24 overlap fixes. Its decisive row is the mixed pileup, whose bases are ACDN*: and whose counts are A=2: the wildcard is counted as an A, while N and : fall out through the -1 check and the deletion through an explicit test. Three exclusions, three mechanisms, one row. The first version of this dump put the wildcard everywhere except the locus, so the claim was unmeasured while appearing covered; the fixture now lands it on the locus and carries a separate byte for the -1 path."
        }
      ]
    },
    {
      "id": "readstates",
      "title": "ReadStateManager sample partition and state advance",
      "status": "oracle-backed",
      "harness": "tools/readfilter-conformance",
      "tools": [],
      "why": "Between the per-read state machines and the pileup sits the layer that decides which reads enter, under which sample, and when they leave. Four of its behaviours change what a locus contains and none is visible from a tool's output. The sample map is a LinkedHashMap, and the reference says so in capitals, so iteration follows the order the samples were declared rather than the header's: the probe declares B before A while the header has A before B. A read whose first stepForwardOnGenome returns null is dropped in silence, so a read that is all soft clips and insertions never reaches any pileup, which upstream marks a todo rather than an error. collectPendingReads takes the left-most *genome position* among the states already in the system, which for a read part-way through a deletion is not where that read started. A read whose sample was not declared at construction is a hard error rather than a new bucket, and a read with no read group at all is the null sample. Downsampling is out of scope on purpose: it draws from Utils.getRandomGenerator, and the port refuses it rather than approximating it.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "ReadStateDump",
          "golden": "crates/gatk-engine/tests/data/read_states.txt.gz",
          "why": "From the pinned container on a real x86-64 runner: 56 traversal steps over 5 runs. Every row of two-samples lists sampleB before sampleA, which is the declaration order and not the header's; all-clipped holds three reads and reports two, because 5S5I never enters; and deletion-boundary admits d2 at 105 while d1 is part-way through a 10D, which is where d1 is rather than where it started."
        }
      ]
    },
    {
      "id": "locusiterator",
      "title": "LocusIteratorByState pileups",
      "status": "oracle-backed",
      "harness": "tools/readfilter-conformance",
      "tools": [],
      "why": "One pileup per covered locus, which is what a LocusWalker iterates, so which elements land where is every locus-based tool's output. Four decisions sit in the twenty lines that build each pileup. The two exclusions are not symmetric: a read whose current operator is N is skipped before the adaptor test, a read whose operator is D is skipped inside it, so a read sitting in an adaptor with a deletion is excluded once by the adaptor while an N is excluded whatever the adaptor says. The adaptor test is per base, so the same read contributes to some loci and not others. The pileup is monolithic and concatenated in sample declaration order, which the reference kept for the HaplotypeCaller and documents as such. And a locus with no surviving element yields no context at all, silently, because emitting empty loci belongs to a different class. All four (includeDeletions, includeNs) combinations are probed, because a tool picks them by overriding two methods whose defaults differ.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "LocusIteratorDump",
          "golden": "crates/gatk-engine/tests/data/locus_iterator.txt.gz",
          "why": "From the pinned container on a real x86-64 runner: 148 pileups over 12 runs. The adaptor rows are the ones that took two attempts to measure: the first fixture put the mate twenty bases before the read, so the boundary (mateStart - 1 for a reverse-strand read) landed before every probed locus and excluded nothing while ten rows appeared to cover it. With the mate five bases into the read, p1 is missing from chr1:101 to chr1:105 and present from 106."
        }
      ]
    },
    {
      "id": "acbuilder",
      "title": "AlignmentContextIteratorBuilder routing and sample order",
      "status": "oracle-backed",
      "harness": "tools/readfilter-conformance",
      "tools": [],
      "why": "Two facts here reach into every locus-based tool's output and neither is visible from the builder's setters. The samples are collected with Collectors.toSet(), so LocusIteratorByState creates its per-sample managers in Java HashSet iteration order and concatenates their elements in that same order to build each pileup: the element order of a multi-sample pileup is a HashSet bucket order over the sample names, deterministic but neither sorted nor the header's. A port using a sorted map, an insertion-ordered list or a different hash would agree on single-sample data and diverge as soon as a second sample appeared, which is why java.util.HashMap's index and its order-preserving resize are ported alongside. And areIntervalsSpecified is intervals != null rather than !isEmpty(), so an empty but non-null list counts as specified, routes to IntervalOverlappingIterator, and that constructor's Utils.nonEmpty throws: an empty interval list is an error rather than an empty traversal, and only when emitEmptyLoci is off.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "AlignmentContextBuilderDump",
          "golden": "crates/gatk-engine/tests/data/ac_builder.txt.gz",
          "why": "From the pinned container on a real x86-64 runner. For the order half this golden is the *definition* rather than a check: the order is a java.util.HashSet's, which is GPL2 and unspecified, so the port reproduces it as an observable and the measurement wins where the two disagree. See docs/an-unspecified-order-that-reaches-the-output.md. The rows earn their place: three sample names come back in the reverse of their insertion order, five numeric names interleave as 11|1|2|3|10, and thirteen names cross the growth point. The routing half shows the empty-list asymmetry: with emitEmptyLoci off an empty non-null interval list is an IllegalArgumentException, and with it on the same list is a traversal of nothing. The contexts of every route are compared too, empty loci included: an uncovered requested locus yields a real context with an empty pileup rather than no context, and the source is advanced past rather than rewound, so a covered locus no requested interval contains is consumed and dropped."
        }
      ]
    },
    {
      "id": "locuswalker",
      "title": "LocusWalker traversal",
      "status": "oracle-backed",
      "harness": "tools/readfilter-conformance",
      "tools": [],
      "why": "The traversal the largest GATK archetype inherits, probed as a real LocusWalker run through the real command line so its defaults are measured rather than transcribed. Four of them are not a ReadWalker's: two default read filters rather than one, WellformedReadFilter and MappedReadFilter, so an unmapped read carrying its mate's position reaches a ReadWalker and never reaches a LocusWalker; includeDeletions true and includeNs false; emitEmptyLoci false, so an uncovered position inside -L produces no apply call at all, which a second walker subclass overriding that method measures against; and --max-depth-per-sample defaulting to 0 with a negative value being a bad argument rather than a synonym for unlimited. The fixture is ReadWalkerDump's, so a divergence between the two suites is a divergence between the traversals rather than between their inputs. The downsampling run is recorded rather than compared: the port refuses that path, and a refusal has to be measured against what the reference actually does.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "LocusWalkerDump",
          "golden": "crates/gatk-tools/tests/data/locus_walker.txt.gz",
          "why": "From the pinned container on a real x86-64 runner: 217 apply calls over 8 traversals. The pair that earns the suite is gap and gap-emptyloci, the same interval run both ways: six calls with the default, fifty-one with emitEmptyLoci overridden, the uncovered ones carrying a real context with zero depth and the reference base still present. One row is honest rather than covering: depth-1 produced rows identical to the undownsampled run, because this fixture never exceeds one read per sample at any locus, so it does not measure downsampling at all. The test asserts that identity so the case cannot quietly start counting as coverage, and a probe that actually downsamples belongs with the java.util.Random port the reservoir needs."
        }
      ]
    },
    {
      "id": "javarandom",
      "title": "java.util.Random sequences",
      "status": "oracle-backed",
      "harness": "tools/readfilter-conformance",
      "tools": [],
      "why": "Utils.getRandomGenerator() is one static new Random(47382911L), seeded at class initialisation, so every draw in a run comes from a single stream and a consumer that takes one value too few or too many shifts that stream for every later consumer as well. The sequence therefore has to be exact before anything is allowed to draw from it, which is why this is its own slice ahead of the downsampler. Unlike HashMap's iteration order, this one is legitimately implementable from the published contract: the Javadoc states the algorithm and its constants and requires every method to produce that exact sequence. Two behaviours are probed for shape rather than value: nextInt(bound) takes a different path for a power-of-two bound, so bound 16 answers differently from what 15 and 17 would suggest, and the general path rejects biased values and redraws, so its draw count depends on the values themselves. A final interleaved sequence mixes the methods on one generator, which catches a port whose methods are individually right but whose draw counts are not.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "JavaRandomDump",
          "golden": "crates/gatk-engine/tests/data/java_random.txt.gz",
          "why": "From the pinned container on a real x86-64 runner: 98 sequences over 7 seeds, plus 7 interleaved streams. The interleaved rows are the ones that would catch a plausible wrong port: a version whose methods are individually correct but whose draw counts are not passes every single-method row and fails those, because nextDouble and nextLong each consume two draws. The seeds include the GATK constant, zero, both extremes of long, and the multiplier itself, which scrambles to zero."
        }
      ]
    },
    {
      "id": "reservoir",
      "title": "ReservoirDownsampler selections",
      "status": "oracle-backed",
      "harness": "tools/readfilter-conformance",
      "tools": [],
      "why": "Which reads a deep pileup keeps. Four behaviours change the answer without being inherent to reservoir sampling. The draw happens even when the slot is discarded: nextInt(totalReadsSeen) is called for every read past the target and only then is the slot compared against it, so a read that is thrown away still advances the shared static stream and every later draw anywhere in the run depends on how many reads were seen here. The probe therefore reports the generator's next value after each case, not only the reads kept: two cases that keep the same reads can leave the stream in different places. The bound grows with the count, so the same read draws differently depending on how many preceded it. Replacement is in place, so the reservoir's order is the order slots were last written rather than the reads' order. And setNonRandomReplacementMode swaps the draw for Math.abs(name.hashCode()) % totalReadsSeen, where Math.abs is wrong for Integer.MIN_VALUE. The generator is reset before each case, so a case measures the downsampler rather than the order the cases ran in.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "ReservoirDownsamplerDump",
          "golden": "crates/gatk-engine/tests/data/reservoir.txt.gz",
          "why": "From the pinned container on a real x86-64 runner. The stream-position rows are the evidence: under-target and nonrandom both leave the generator at 1057280359, its first value, because neither took a draw, while one-over leaves it at -873351126, its second, because it took exactly one before discarding the slot. A port that skipped the draw when the slot loses would keep the same reads and fail those rows."
        }
      ]
    },
    {
      "id": "well19937c",
      "title": "Well19937c sequences",
      "status": "oracle-backed",
      "harness": "tools/readfilter-conformance",
      "tools": [],
      "why": "GATK carries two static generators, not one: Utils.getRandomGenerator() is a java.util.Random and Utils.getRandomDataGenerator() is a RandomDataGenerator over a Well19937c, seeded from the same constant and reset together. They are different algorithms with different streams, and LevelingDownsampler draws from the second where ReservoirDownsampler draws from the first, so a port that routed both through one generator would agree with neither. Four shapes are probed because a port gets them wrong while looking right. The two constructors disagree: new Well19937c(long) splits the seed into two ints, high word first, so it seeds a different 624-word pool from new Well19937c((int) seed) even when the long fits in an int, and GATK uses the long one. The derived methods are not java.util.Random's: nextDouble is ((next(26) << 26) | next(26)) * 2^-52 where java.util.Random uses 27 bits and 2^-53, nextFloat takes 23 bits rather than 24, and nextLong ors its halves where java.util.Random adds them signed. nextInt(bound) is the one that does agree, because commons-math copied it from Apache Harmony, so bounds either side of 16 are dumped to confirm the power-of-two path and the rejection loop. And nextBytes takes one extra full draw after its four-at-a-time loop even when the length is a multiple of four and nothing is left to fill, so lengths 0 through 9 are dumped with the generator's next value after each. nextGaussian is deliberately absent from both the dump and the port: it goes through commons-math's own FastMath, whose cos, sin, log and sqrt are its implementations rather than the platform's, so a port of its shape would be bit-wrong in the mantissa while looking right. It belongs with jmath.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "Well19937cDump",
          "golden": "crates/gatk-engine/tests/data/well19937c.txt.gz",
          "why": "From the pinned container on a real x86-64 runner. The rows that matter most are the interleaved ones and the nextBytes rows: the first catch a port whose methods are individually right but whose draw counts are not, and the second catch the unconditional trailing draw, which is invisible in the bytes themselves and visible only in where the stream ends up."
        }
      ]
    },
    {
      "id": "leveling",
      "title": "LevelingDownsampler and the permutation underneath it",
      "status": "oracle-backed",
      "harness": "tools/readfilter-conformance",
      "tools": [],
      "why": "LevelingDownsampler cuts several stacks down to one total, and it removes items through MathUtils.sampleIndicesWithoutReplacement, which is RandomDataGenerator.nextPermutation over the static Well19937c. Both layers decide the answer, so both are dumped. nextPermutation(n, k) shuffles all n entries and returns the first k, so it costs n - 1 draws whatever k is: sampling 2 of 1000 spends 999 draws and moves the stream for every later consumer, which is why the probe reports the generator's next value after each call. Its shuffle runs downwards with shrinking bounds, and its last step takes no draw at all, so the cost is n - 1 rather than n. The indices it returns are the shuffled head and are therefore unsorted, and since the caller uses them only as membership, the raw list is dumped so that a port which sorted them still fails. Above that, the leveling plan is pure arithmetic: a round-robin walk over the sizes decrements one at a time, so how many go from each stack is fixed before any draw and only which ones is random. The walk stops on consecutive refusals rather than on a scan. A stack that keeps everything takes no draw, so stack sizes shift the stream for the stacks after them. LinkedList and ArrayList take different removal paths upstream and must keep the same items, so both are dumped and a divergence between them is a failure rather than an assumption. And with minElementsPerStack of zero a stack can be planned down to nothing, at which point nextPermutation throws NotStrictlyPositiveException instead of returning an empty selection.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "LevelingDownsamplerDump",
          "golden": "crates/gatk-engine/tests/data/leveling.txt.gz",
          "why": "From the pinned container on a real x86-64 runner. The stream-position rows are the evidence for the draw counts, and the target-zero pair is the evidence for the refusal: with minElementsPerStack one the stacks fall to one item each, and with zero the same target reaches nextPermutation(n, 0) and throws."
        }
      ]
    },
    {
      "id": "variantwalker",
      "title": "VariantWalker traversal",
      "status": "oracle-backed",
      "harness": "tools/readfilter-conformance",
      "tools": [],
      "why": "The traversal every variant-based tool inherits, run through the real command line rather than reconstructed. Reading FeatureIntervalIterator suggests that -L can produce duplicates: featureIsNovel tests a feature against previousInterval alone, one interval of memory rather than a set, so a variant covered by intervals 1 and 3 but not by 2 would reach apply twice. The suite measures that and the answer is no: three intervals whose first and third are identical produce two apply calls rather than four, and intervals given out of order are traversed in sorted order. The argument layer sorts and merges before the traversal sees the list, so the precondition the iterator documents is always satisfied through a command line. The behaviour is real in the class and unreachable in the tool, which is a different claim from saying it does not happen, and the distinction is exactly what a golden is for. And an empty interval list is not an empty traversal: setIntervalsForTraversal maps both null and an empty list to null, which means no restriction at all. The record fields travel with each apply call because the walker is also where a decoded record becomes what a tool sees: the interval it builds is SimpleInterval(variant), whose end is the END attribute when the record carries one and start plus the reference allele's length minus one otherwise.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "VariantWalkerDump",
          "golden": "crates/gatk-tools/tests/data/variant_walker.txt.gz"
        }
      ]
    },
    {
      "id": "activityprofile",
      "title": "Where an assembly region starts and stops",
      "status": "oracle-backed",
      "harness": "tools/readfilter-conformance",
      "tools": [],
      "why": "The boundaries HaplotypeCaller and Mutect2 assemble over: a boundary that moves by one base changes the haplotypes and therefore the calls, so this is upstream of every variant those tools emit. Four behaviours decide it and none follows from cutting where the activity changes. A probability is spread over a Gaussian and the spread is added rather than assigned, so the probability at a site is the sum of its neighbours' tails and a site never reported active can cross the threshold, while a probability of exactly zero skips the filter entirely. The filter width is chosen from the kernel's values rather than from sigma, and it feeds back into getMaxProbPropagationDistance, which decides when a region may be popped at all. The cut site is a local minimum searched backwards, and isMinimum is asymmetric, requiring p[i] <= p[i+1] but p[i] < p[i-1], so a plateau is cut at its left-hand end. And nothing may be popped until the profile holds maxRegionSize plus the propagation distance in states, so regions appear in bursts rather than as soon as their activity ends. The kernels are compared as raw bits because the last bits are the point: Math.exp is the platform's and a port whose exp differs in the final ulp moves a cut site.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "ActivityProfileDump",
          "golden": "crates/gatk-engine/tests/data/activity_profile.txt.gz"
        }
      ]
    },
    {
      "id": "assemblyregion",
      "title": "The assembly region itself: spans, reads, trimming",
      "status": "oracle-backed",
      "harness": "tools/readfilter-conformance",
      "tools": [],
      "why": "What HaplotypeCaller and Mutect2 actually assemble, once the activity profile has decided where a region starts and stops: a primary span for calling, a padded span for assembly, and the reads overlapping the padded one. Four behaviours are measured because none is what the class says. The padding constructor goes through IntervalUtils.trimIntervalToContig, which returns null rather than throwing when the padded interval cannot be placed on the contig, and the null then reaches Utils.nonNull inside the other constructor, so the failure is reported as a null padded span and never mentions padding. trim(span, padding) does not produce the region its own javadoc describes: the javadoc works an example, active 100-200 padded 50-250 trimmed to 150-225 giving a region 150-200 with 25bp of padding, but the code expands the requested span by the requested padding and intersects that with the old padded span, so the padding is never recomputed to fit. Trimming re-clips every read to the new padded span, drops the ones left empty or no longer overlapping, and then sorts the survivors with ReadCoordinateComparator, so the read order of a trimmed region is not the order the reads were added in and the comparator is observable output rather than an internal detail. And trimming drops the hard-clipped pileup reads entirely, because the new region is constructed empty and only addAll is called on it. The comparator is dumped over every ordered pair of the corpus, so its antisymmetry is measured too: a comparator that is not antisymmetric sorts differently under a different sort algorithm.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "AssemblyRegionDump",
          "golden": "crates/gatk-engine/tests/data/assembly_region.txt.gz"
        }
      ]
    },
    {
      "id": "intervaliterators",
      "title": "The iterators between a shard of reads and an activity profile",
      "status": "oracle-backed",
      "harness": "tools/readfilter-conformance",
      "tools": [],
      "why": "LocusIteratorByState yields nothing at all for a locus no read covers, so a position with zero depth is silently absent from its output. The assembly-region traversal cannot work that way, because the activity profile has to see the gap in order to close a region over it, and AssemblyRegionIterator therefore wraps the locus iterator in IntervalAlignmentContextIterator, whose whole job is to walk every base of every interval and manufacture an empty AlignmentContext wherever the wrapped iterator has none. The comment upstream is unusually direct about the stakes: this is critical for reproducing GATK 3.x behavior. A port that skipped the wrapper would produce regions ending at the last covered base instead of where the activity dies, which moves every boundary downstream of a coverage gap. Underneath it, ShardedIntervalIterator computes shard indices over the interval length and converts them back to coordinates, so the arithmetic is absolute rather than relative and the last shard is truncated against the interval end; IntervalLocusIterator is that class at shard size 1. The corpus has deliberate gaps before, inside and after its reads, and the intervals asked for start before the coverage, end after it, sit entirely inside a gap, and cross a contig boundary.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "IntervalIteratorsDump",
          "golden": "crates/gatk-engine/tests/data/interval_iterators.txt.gz"
        }
      ]
    },
    {
      "id": "assemblyregioniterator",
      "title": "The assembly-region traversal: loci in, regions out",
      "status": "oracle-backed",
      "harness": "tools/readfilter-conformance",
      "tools": [],
      "why": "Where the activity profile, the assembly region and the locus iterators meet, and what HaplotypeCaller and Mutect2 are once the assembler is removed: a region boundary that moves by one base here changes their calls. Four behaviours are measured and three are invisible from any tool output. A shard carries two interval lists built the same way, the padded one with the padding and the unpadded one with zero, and passing zero is not a no-op because getIntervalsWithFlanks sorts and merges with IntervalMergingRule.ALL whatever the padding is, so the reads are queried over one list while the loci are walked over another. The profile is popped before the current pileup is added and the force flag is that this locus does not continue the profile, which the upstream comment insists on: adding first would make the profile contiguous with the new pileup, the force would never fire, and a region would never be closed at a gap between two intervals. A region popped from the profile is not ready, but waits in a queue until the loci have advanced past the end of its padded span, because until then the reads that belong in it have not been read. And reads are carried forward from the previous region as well as taken from the cache, so a read spanning a boundary is in both regions, while a read is left in the cache only when its start is past the region padded end. The evaluator is a probe rather than a caller, activity being a declared function of pileup depth, so the regions are decided by the traversal and not by anything statistical.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "AssemblyRegionIteratorDump",
          "golden": "crates/gatk-engine/tests/data/assembly_region_iterator.txt.gz"
        }
      ]
    },
    {
      "id": "assemblyregionwalker",
      "title": "What an AssemblyRegionWalker hands to apply()",
      "status": "oracle-backed",
      "harness": "tools/readfilter-conformance",
      "tools": [],
      "why": "The last of the five walker archetypes and the one HaplotypeCaller and Mutect2 are built on. The probe is a real AssemblyRegionWalker run through the real command line rather than a reconstruction, so its defaults are measured rather than transcribed: min 50, max 300, padding 100, threshold 0.002, propagation 50, from an argument collection the walker instantiates without overriding anything. Four behaviours are the point and three are invisible from any tool output. There is one read shard per contig rather than one per interval, so two -L arguments on the same contig share a shard and an activity profile and can end up in one region if the padding joins them, while two on different contigs cannot whatever their distance. apply receives its reference and feature contexts over the region padded span rather than its active span, so a tool reads bases outside the territory it may call in, which is the point of the padding. force-active rewrites isActive after the iterator has produced the region, so it marks every region active without moving a single boundary, where a port folding it into the evaluator would return one enormous region instead. And the default read filters are WellformedReadFilter and MappedReadFilter, the same pair as a LocusWalker and not a ReadWalker single filter, so an unmapped read parked at its mate coordinate never reaches this traversal. The fixture is the read walker one, so a divergence between these suites is a divergence between traversals rather than between inputs.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "AssemblyRegionWalkerDump",
          "golden": "crates/gatk-engine/tests/data/assembly_region_walker.txt.gz"
        }
      ]
    },
    {
      "id": "mann-whitney",
      "title": "The rank-sum test",
      "status": "oracle-backed",
      "harness": "tools/genotyper-conformance",
      "tools": [],
      "why": "MannWhitneyU is the test behind BaseQRankSum, MQRankSum, ReadPosRankSum and ClippingRankSum, and four of its decisions are not what a rank-sum test suggests. The ranks are floats: the values are doubles while the ranks, their sums and the averaged rank of a tie band are all single precision, so a port that ranked in double precision would agree on small inputs and drift on realistic ones. Two different tests are chosen by size, with either series at ten or more taking the normal approximation and anything smaller enumerating every permutation of the group labels, so the reported Z comes from a cumulative probability in one regime and from an inverse cumulative probability in the other, and commons-math3's two are not inverses of each other. transformTies reports zero ties when every single value is tied, because the sigma formula breaks down there, and the continuity correction is then dropped as well, so two mechanisms conspire to produce exactly one half. And the permutation p-value is half the observed bin plus everything more extreme rather than the cumulative distribution, because the cumulative form gives a p-value of one in the most extreme case and no usable Z.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "MannWhitneyDump",
          "golden": "crates/gatk-engine/tests/data/mann_whitney.txt.gz",
          "why": "From the pinned container on a real x86-64 runner. 69 test results and 162 Z scores, bit-identical on the first run, which covers both regimes: the permutation test at nine and nine, the normal approximation one element later, and the float rank sums at three hundred a side where single precision stops being exact."
        }
      ]
    },
    {
      "id": "annotation-rank-sum",
      "title": "BaseQRankSum, MQRankSum, ReadPosRankSum and ClippingRankSum",
      "status": "oracle-backed",
      "harness": "tools/annotation-conformance",
      "tools": [],
      "why": "The four rank-sum annotations run a Mann-Whitney U test of the alternate reads against the reference reads and report a Z score, and four things decide what a consumer sees. The alternate goes first, so swapping the two arrays would flip the sign of every value this family reports and nothing downstream would notice. The value is a String, formatted with a three-decimal format that rounds half up on the decimal expansion rather than however a Double would render, so the VCF carries text and not a number. A NaN Z score is not written at all, which means a site with no alternate reads has no MQRankSum field rather than one reading zero. And three filters run before a read contributes, one of which ReadPosRankSum overrides to add a soft-clip test against the variant's end plus one, so the four members do not see the same reads.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "RankSumAnnotationDump",
          "golden": "crates/gatk-annotation/tests/data/rank_sum.txt.gz",
          "why": "From the pinned container on a real x86-64 runner. 56 answers, 21 of them an absent key rather than a value, and no corrections. The absent ones are what the suite is for: null likelihoods, no genotypes, an empty matrix, and a site whose alternate series is empty all produce no field at all rather than a field reading zero."
        }
      ]
    },
    {
      "id": "annotation-strand-bias",
      "title": "FS, SOR and SB",
      "status": "oracle-backed",
      "harness": "tools/annotation-conformance",
      "tools": [],
      "why": "The three strand-bias annotations read one 2x2 contingency table three ways, and four things decide what a consumer sees. The table is built per sample and only then added up, and a sample under the threshold contributes nothing rather than contributing to a pooled total that would pass, with FS using a minimum count of two and SOR of zero, so the two do not see the same table. The genotype field wins: if any genotype carries SB, both are computed from those per-sample arrays and the likelihood matrix is never consulted, which makes SB load-bearing rather than diagnostic. FS normalises a table above four hundred reads by scaling to about two hundred and truncating to int, so a deep site is scored from a table that no longer sums to its coverage, while SOR does not normalise at all. And FS is phred-scaled off a floor of 1e-320, which exists to prevent infinities, so the largest value a site can report is about 3200 whatever the evidence. The p-value itself goes through Math.pow, which decision 0007 deferred, so this suite is also where the port learns whether the platform's pow agrees with the reference's on these inputs.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "StrandBiasAnnotationDump",
          "golden": "crates/gatk-annotation/tests/data/strand_bias.txt.gz",
          "why": "From the pinned container on a real x86-64 runner. 87 rows, no corrections, and sixteen of them are Fisher p-values that pass through Math.pow(10, x). Decision 0007 deferred that function because its intrinsic is GPL2 and not correctly rounded; the port calls the platform's powf, and on every input these annotations produce the two agree bit for bit. That is evidence, not a proof: it extends the pow measurement of decision 0007 rather than closing it."
        }
      ]
    },
    {
      "id": "annotation-read-grouping",
      "status": "oracle-backed",
      "title": "AS_UNIQ_ALT_READ_COUNT, BQHIST and REF_BASES",
      "harness": "tools/annotation-conformance",
      "tools": [],
      "why": "Three annotations whose output shape is more interesting than their arithmetic. AS_UNIQ_ALT_READ_COUNT counts distinct start and fragment-length pairs rather than reads, so a hundred PCR duplicates of one fragment count once and two genuinely different fragments sharing both numbers also count once, and the value is a String joined with a pipe because it is an allele-specific raw annotation. BQHIST is a flat list of one entry per distinct quality followed by one count per allele of the matrix in matrix order, so its length depends on the matrix rather than on the variant and reading it requires knowing that number. And REF_BASES is twenty-one bases taken out of whatever window the reference context carries, with the discard clamped at zero and the right end padded with N, so the string has a fixed length and is not necessarily centred on the variant.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "ReadGroupingAnnotationDump",
          "golden": "crates/gatk-annotation/tests/data/read_grouping.txt.gz",
          "why": "From the pinned container on a real x86-64 runner. 24 rows, no corrections. The three-duplicates row is the one that carries the claim: three reads, one fragment, one count."
        }
      ]
    },
    {
      "id": "annotation-depth-per-allele",
      "title": "AD, AF, DP and the marginalisation under them",
      "status": "oracle-backed",
      "harness": "tools/annotation-conformance",
      "tools": [],
      "why": "AD, AF and the HaplotypeCaller's DP are all counted off a marginalised likelihood matrix, and the marginalisation's allele order is a HashMap's. Collectors.toMap builds one and marginalize takes its key set as the new allele array, so the order follows Allele.hashCode, which is the bases' array hash times thirty-one plus the boolean hash of the reference flag. That order is observable, because searchBestAllele breaks a tie by keeping the first index, so a read supporting two alleles equally is attributed to whichever the hash put first. marginalize itself takes the maximum likelihood of the old alleles a new one stands for rather than their sum. AD is keyed on the variant's alleles and counted over the matrix, so an allele the matrix holds and the variant does not is counted into a bucket nobody reads. AF drops the first entry, so it is one shorter than AD and the two do not line up by index. And DP counts informative reads only, so it can be lower than the INFO-level DP over the same site.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "DepthPerAlleleDump",
          "golden": "crates/gatk-annotation/tests/data/depth_per_allele.txt.gz",
          "why": "From the pinned container on a real x86-64 runner. 41 rows. The order rows are the point: A*,C comes out C,A* and A*,C,G,T comes out T,C,A*,G, so the marginalised matrix does not follow the variant's allele order. The golden corrected the port on one thing, normalizeSumToOne, which has no zero guard: an all-zero AD divides zero by zero, so a site with no informative reads gets an AF of NaN rather than of zero. Two of those rows are an invalid operation's NaN whose sign is the FPU's, exempted off x86-64 under decision 0012."
        }
      ]
    },
    {
      "id": "annotation-flow",
      "title": "The eight flow-based annotations",
      "status": "oracle-backed",
      "harness": "tools/annotation-conformance",
      "tools": [],
      "why": "The flow annotations describe a variant in flow space rather than in base space, and the encoding is where their surprises are. A flow key is a run-length encoding against a repeating flow order, TGCA by default, and N matches every flow base, so an ambiguous base is absorbed into whatever run it sits in rather than breaking it; a base the order does not contain trips a period guard and throws. An hmer indel is one whose reference and alternate flow keys differ in exactly one flow, with the length reported being the larger of the two and the nucleotide the order's base at that index, so a second differing flow disqualifies the site entirely. VARIANT_TYPE asks three questions in order and one ordinary indel beside an hmer one makes the whole site non-h-indel. A motif that would run off the contig is not truncated but dropped, so the annotation is absent rather than short. The left motif shifts for an indel, dropping its first base and appending the reference's first, so the two motifs of a mixed site come from different windows. And GC_CONTENT is a float rather than a double, so it carries single precision into the VCF.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "FlowAnnotationDump",
          "golden": "crates/gatk-annotation/tests/data/flow.txt.gz",
          "why": "From the pinned container on a real x86-64 runner. 117 rows, and the golden corrected the port on four points, all of them about what happens at the edge of a reference window. A symbolic allele has length zero rather than the length of its text, so <NON_REF> classifies as a deletion. The two motifs are written into the attribute map independently, so a left motif that ran off the window leaves the right one in place. CycleSkipStatus declares that an actual flow order is required and therefore normally produces nothing, but it dereferences the motif lists before that check, so it throws a NullPointerException where the left motif is missing. And where the right motif's attribute was not written but its list survives with nulls in it, Java concatenates the four characters null into the sequence, whose characters are not bases, so the flow key's period guard trips and the exception is a GATKException instead."
        }
      ]
    },
    {
      "id": "annotation-site-statistics",
      "title": "QD, the genotype summaries and LikelihoodRankSum",
      "status": "oracle-backed",
      "harness": "tools/annotation-conformance",
      "tools": [],
      "why": "QD above thirty-five is randomised: the reference replaces it with thirty plus a Gaussian jitter, so the value written to a VCF is a draw from a seeded generator rather than a function of the data. The port refuses that branch instead of approximating it, because nextGaussian goes through StrictMath.log, which is fdlibm and differs from the correctly-rounded logarithm this programme has on 186 of the corpus's 44,996 points. The suite therefore measures where the boundary is and what the raw ratio was, not what lies past it. The depth QD divides by is also not DP: only het and hom-var genotypes count, the whole AD total is added for each, and a separate AD-restricted tally collects the same totals only where the alternate part exceeds one, and if that tally ends non-zero it replaces the depth entirely, so one sample with two alternate reads can discard every other sample's depth. GQ_MEAN and GQ_STDDEV are strings and the deviation is written only with more than one GQ. NCC counts no-call alleles rather than no-call samples. And LikelihoodRankSumTest is the one member of the rank-sum family whose value comes from the matrix rather than from the read.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "SiteStatisticsDump",
          "golden": "crates/gatk-annotation/tests/data/site_statistics.txt.gz",
          "why": "From the pinned container on a real x86-64 runner. 30 rows, of which two are QD values the reference randomised: the suite asserts that the port refuses exactly those, that the raw ratio really was at or past thirty-five, and that what the reference wrote is not the ratio. The golden corrected the port on one point: getNoCallCount counts no-call genotypes rather than the no-call alleles inside them, so a diploid no-call sample contributes one and not two."
        }
      ]
    },
    {
      "id": "annotation-heterozygosity-and-mq",
      "title": "ExcessHet, InbreedingCoeff, MQ, MQ0 and the tandem repeats",
      "status": "oracle-backed",
      "harness": "tools/annotation-conformance",
      "tools": [],
      "why": "ExcessHet and InbreedingCoeff ask the same question of a cohort and answer it from the same three numbers, computed by one function called with opposite rounding flags, so the two annotations on one site can disagree about how many hets there are and both are right. The rounded branch guards against counting a genotype twice by comparing two normalised likelihoods for exact floating-point equality, which is reachable from PLs of [0,0,X] and [X,0,0], and a hom-ref genotype with no likelihoods at all is given a distribution invented from its GQ in three different ways. ExcessHet saturates below a p-value of 10e-60, which is 1e-59 and not 1e-60, so values above the saturating 160.0000 do occur. MQ drops reads whose mapping quality is 255 from both the numerator and the denominator, so an all-unavailable matrix divides by zero and the annotation is the four characters NaN, while MQ0 answers zero for an empty matrix where every other annotation in this crate answers nothing. And findRepeatedSubstring cannot see a partial trailing repeat, because Arrays.copyOfRange pads with zero bytes past the end of the array and a zero byte is not a base.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "HeterozygosityAndMqDump",
          "golden": "crates/gatk-annotation/tests/data/heterozygosity_and_mq.txt.gz",
          "why": "From the pinned container on a real x86-64 runner. 128 rows: 40 genotype-count triples compared as raw bits, 40 cohort annotations, 27 mapping-quality answers and 21 repeat measurements. The counts are bits rather than decimals because they are what the two annotations divide and index by, and the first candidate golden earned that choice: it caught the port calling MathUtils.log10sumLog10 where normalizeLog10 calls log10SumLog10, one letter apart and two ulp apart, on the equilibrium cohort's het count and on nothing else."
        }
      ]
    },
    {
      "id": "annotation-allele-specific-rank-sum",
      "title": "The three allele-specific rank sums, and the histogram they travel as",
      "status": "oracle-backed",
      "harness": "tools/annotation-conformance",
      "tools": [],
      "why": "The direct annotate() path of an allele-specific annotation is not allele-specific: it pools every alternate's reads into one series and reports one Z score, exactly as its non-allele-specific parent does, under a different key. The allele-specific part lives entirely in the raw gVCF path, where the raw string starts with its delimiter because the reference allele is skipped but its slot is not, and the parser reads the slots positionally, so a raw string whose leading delimiter were trimmed would shift every allele by one. A site with no unambiguous reference read produces a string of bare delimiters rather than a missing value per allele. And each Z score is stored as a one-entry histogram binned to a tenth by a floor, so a negative score bins away from zero, the binning is not symmetric about it, and the joint-called AS_MQRankSum is the median of those bins rather than the rank sum of the pooled reads.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "AlleleSpecificRankSumDump",
          "golden": "crates/gatk-annotation/tests/data/allele_specific_rank_sum.txt.gz",
          "why": "From the pinned container on a real x86-64 runner. 100 rows: 11 histograms, 5 compressed lists, 16 binnings one value at a time, 27 direct and 27 raw allele-specific answers, and 7 combinations with their finalisations. The binning rows are what make the claim that a negative score bins away from zero a measurement rather than a reading of the source, and the ref-only row is what showed that a site with reference reads and no alternate read writes the four characters NaN into the raw field: the rank sum of an empty series is NaN, Histogram.add drops it, and an empty histogram renders as Double.toString(Double.NaN)."
        }
      ]
    },
    {
      "id": "annotation-allele-specific-strand-bias",
      "title": "AS_FS, AS_SOR and the strand table they share",
      "status": "oracle-backed",
      "harness": "tools/annotation-conformance",
      "tools": [],
      "why": "AS_SB_TABLE writes an entry for the reference too and puts the delimiter between entries, so it does not start with one, while the allele-specific rank sums skip the reference's value but keep its slot and theirs does: two families, two conventions for the same delimiter, and this parser refuses a count mismatch outright where the other pads. A sample contributes nothing at all unless its whole table holds more than two informative reads, so a sample with exactly two is dropped even for the allele that had both of them. AS_StrandOddsRatio computes a value for the reference allele against itself and then never prints it, while AS_FisherStrand filters the reference out first, and the two siblings differ in exactly that line. AS_FS floors its p-value at 1e-320, which is a subnormal, before phred-scaling it. And a reduced string skips an alternate the annotation data does not carry rather than writing the missing value, so the field can come out with fewer entries than the variant has alternates.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "AlleleSpecificStrandBiasDump",
          "golden": "crates/gatk-annotation/tests/data/allele_specific_strand_bias.txt.gz",
          "why": "From the pinned container on a real x86-64 runner. 76 rows: 14 raw strand tables, 28 direct answers, 9 combinations with their 16 finalisations, and 8 phred scalings compared as raw bits. The golden corrected the port twice. An entry that is present but empty is not the same as an absent one: the combined field reads 10,8| with nothing after the delimiter, where the port had written 0,0. And the allele-specific minimum count is two for both members, while the plain StrandOddsRatio uses zero, so SOR and AS_SOR disagree on a site whose only sample has one or two informative reads."
        }
      ]
    },
    {
      "id": "annotation-allele-specific-site-statistics",
      "title": "AS_QD, AS_MQ, AS_InbreedingCoeff and the heterozygosity calculator",
      "status": "oracle-backed",
      "harness": "tools/annotation-conformance",
      "tools": [],
      "why": "AS_QD has no raw data of its own: annotateRawData returns an empty map, its raw key is expected to be written by GenotypeGVCFs, and annotate is finalizeRawData with the same variant context passed twice. It adds the reference depth to every alternate's denominator, so a triallelic site's denominators overlap and the entries sum to nothing meaningful, and it inherits fixTooHighQD once per alternate, so one entry of a comma-separated field can be deterministic and the next drawn from a random generator. AS_MQ divides a sum of squares taken from the likelihood matrix by an AD summed from the genotypes, with nothing checking that the two describe the same reads, and its depth counts every genotype where AS_QD's counts only het and hom-var genotypes whose alternate depth exceeds one. AS_InbreedingCoeff is the only member that is not reducible and does not inherit from its non-allele-specific sibling, and its heterozygosity calculator adds the hom-ref mass to the reference's count once per alternate allele, so a triallelic site counts it twice.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "AlleleSpecificSiteStatisticsDump",
          "golden": "crates/gatk-annotation/tests/data/allele_specific_site_statistics.txt.gz",
          "why": "From the pinned container on a real x86-64 runner. 71 rows: 12 AS_QD answers of which three are values the reference randomised and the port refuses, 12 allele-depth vectors, 21 mapping-quality answers, and 14 heterozygosity rows whose counts are compared as raw bits. The golden corrected the port twice. AS_QUAL is read with getAttributeAsList, which does not split a comma-separated string, so a triallelic site annotated programmatically with the one String \"70,140\" throws rather than reading two qualities. And AS_RMSMappingQuality collects into a bare ReducibleAnnotationData that nothing pre-populates, so an allele with no informative read has no key and is skipped entirely, while the parsed form is pre-populated with nulls and writes the missing value for the same state."
        }
      ]
    },
    {
      "id": "annotation-pedigree",
      "title": "The de novo calls and the transmitted singletons",
      "status": "oracle-backed",
      "harness": "tools/annotation-conformance",
      "tools": [],
      "why": "TransmittedSingleton reads the child's depth three times and names the three results childIsHighDepth, momIsHighDepth and dadIsHighDepth, so the parents' depths are never looked at and the annotation's own documented caveat about all three samples being deep is not what the code does. Genotype.getDP is minus one when absent rather than zero, and PossibleDeNovo's depth threshold defaults to zero, so a trio with no DP fails the high-confidence branch and falls into the low one, which does not test depth at all. isViolation indexes the child's alleles at zero and one and asks each parent to contain one of them rather than to be able to transmit it, so a half-called parent transmits whatever it does carry and only an entirely uncalled one reaches the special branches above. The de novo allele-frequency cutoff is the larger of a flat four samples and a thousandth of the cohort, so the flat four wins below four thousand samples. And the call-rate gate counts genotypes whose GQ is strictly greater than twenty where every other threshold in the pair is greater-or-equal.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "PedigreeAnnotationDump",
          "golden": "crates/gatk-annotation/tests/data/pedigree.txt.gz",
          "why": "From the pinned container on a real x86-64 runner. 34 rows: 6 genotype types, 10 Mendelian verdicts, 10 de novo answers and 8 singleton answers, all identical on the first run. Three rows are the two depth facts: shallow-child passes the high-confidence branch because the default depth threshold is zero and a depth of zero clears it, no-depth fails it because an absent DP reads as minus one, and shallow-parents is still emitted because all three of TransmittedSingleton's depth tests read the child."
        }
      ]
    },
    {
      "id": "annotation-fragment-counts",
      "title": "F1R2, F2R1, FAD and the fragment grouping under them",
      "status": "oracle-backed",
      "harness": "tools/annotation-conformance",
      "tools": [],
      "why": "groupEvidence sums the log likelihoods of a group rather than averaging them, so a pair whose reads each support an allele at minus one gives the fragment a minus two and the informativeness threshold, which is an absolute difference, is easier to clear after grouping than before. The new evidence order is a HashMap's over the read names, because groupingBy builds one. The pair's orientation, its usability and its base quality are all read off the first read of the fragment, which is the first in the sample's own order, so which read is consulted depends on the order the reads were added and a pair whose second read is poor is still counted. isF2R1 is isReverseStrand equals isFirstOfPair, and an unpaired read has isFirstOfPair false, so a forward unpaired read lands in F2R1 and a reverse one in F1R2, the opposite of what the names suggest. And the counts are keyed on the matrix's alleles and read back by the variant's, so an allele the variant declares and the matrix does not is a NullPointerException rather than a zero.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "FragmentCountsDump",
          "golden": "crates/gatk-annotation/tests/data/fragment_counts.txt.gz",
          "why": "From the pinned container on a real x86-64 runner. 48 rows: 12 fragment lists, 12 grouped likelihood tables compared as raw bits, 12 orientation count pairs and 12 fragment depth vectors, all identical on the first run. The three-reads-one-name row is the sharpest pair: the Fragment holds two reads because the supplementary alignment was dropped, while its likelihood is the sum over three, because the grouping summed the whole group before the fragment was built. The trimming applies to the evidence object and not to the arithmetic."
        }
      ]
    },
    {
      "id": "annotation-haplotype-filtering",
      "status": "oracle-backed",
      "title": "HaplotypeFilteringAnnotation: ASSEMBLED_HAPS and FILTERED_HAPS",
      "harness": "tools/annotation-conformance",
      "tools": [],
      "why": "The fifty-second annotation, and the first JumboInfoAnnotation: it is handed three likelihood matrices and reads only the third, whose allele axis is a Haplotype rather than an Allele. That is what it was waiting on, not jmath. ASSEMBLED_HAPS is alleles().size() over an IndexedSet under Haplotype.equals, which compares the uniqueness value, the reference flag and the bases while Haplotype.hashCode compares the bases alone, so two haplotypes with identical bases are one entry or two depending on a field that never appears in the output; the duplicate-bases-same-uniqueness and duplicate-bases-different-uniqueness pair is what pins it. FILTERED_HAPS is a getter over an int field that only AlleleFiltering.subsetHaplotypesByAlleles ever writes, so a matrix nobody filtered reports zero rather than no key at all, and the getter does not clamp a value that exceeds the haplotypes remaining or a negative one. Neither key is ever guarded: the method has no branch. And the engine chooses between an AlleleLikelihoods over Fragment and one over GATKRead with a ternary, a difference Java erases behind a wildcard, so both branches are run over the same haplotypes and compared.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "HaplotypeFilteringAnnotationDump",
          "golden": "crates/gatk-annotation/tests/data/haplotype_filtering.txt.gz",
          "why": "From the pinned container on a real x86-64 runner, published as the golden-pending artefact of the run that added this suite. 15 rows, no corrections. The pair that carries the claim is duplicate-bases-same-uniqueness against duplicate-bases-different-uniqueness: the same bases, one entry against two, decided by a field that never appears in the output."
        }
      ]
    },
    {
      "id": "multipasswalker",
      "status": "oracle-backed",
      "title": "The multi-pass walkers, and what they do between passes",
      "harness": "tools/readfilter-conformance",
      "tools": [],
      "why": "The last unported walkers of G1.6. Every other walker makes one traversal; these make several, and the three things they do between passes are each invisible from a single-pass run. MultiplePassVariantWalker builds one CountingVariantFilter and one CountingReadFilter before the loop and reuses both, so the filtered counts accumulate: the same file and the same predicate report one drop after a one-pass run, two after two passes and three after three. MultiplePassReadWalker builds a new filter at the top of every pass after the first and resets the reads data source, so each pass reports only its own drops; the two classes are one directory apart and disagree. afterNthPass is called inside the loop, so it runs after every pass including the last, which its own javadoc contradicts. TwoPassVariantWalker routes afterNthPass(0) to afterFirstPass and guards with else-if n greater than one, so n equal to one matches neither branch and the call after the second pass does nothing at all: there is no afterSecondPass and no error either. And zero passes is a legal traversal that visits nothing, calls afterNthPass never, and still builds both filters.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "MultiPassWalkerDump",
          "golden": "crates/gatk-tools/tests/data/multi_pass.txt.gz",
          "why": "From the pinned container on a real x86-64 runner, published as the golden-pending artefact of the run that added this suite. 144 rows, no corrections. The rows that carry the claim are the filter counts: npass-1, npass-2 and npass-3 report 1, 2 and 3 over one counter, while read-three-pass reports 3, 3, 3 over three."
        }
      ]
    },
    {
      "id": "barclay-value-model",
      "status": "oracle-backed",
      "title": "Barclay's argument value model",
      "harness": "tools/argument-conformance",
      "tools": [],
      "why": "G1.8's first slice: the layer a covering-array vector is interpreted by, run at library level with no tool around it. Six of its rules are not what the annotation names suggest. optional() does not decide whether an argument is optional: isOptional is the annotation OR a default that renders as something other than the string null, and an empty collection renders as null, so an initialised-but-empty List is required while an initialised-and-non-empty one is not. The string null is a value with three outcomes: it clears a collection, it throws on a non-optional argument, and it throws a different exception on a scalar whose raw field is primitive, which is a different question from the boxed class the range check asks. isValueOutOfRange begins with value equals null, so a null on a bounded numeric argument is an out-of-range error rather than an accepted null, and the message formats the bounds as integers only when the value is an Integer, so the same argument reports allowed range [1, 10] for a rejected zero and allowed range [1.0, 10.0] for a rejected null. checkArgumentRange tests the recommended range with that same method, which compares against the hard bounds, so the recommended-range warning can fire only for a null and is otherwise unreachable. A scalar refuses a second occurrence rather than taking the last one. And the grammar is jopt-simple's rather than Barclay's: the equals spelling is refused outright, a flag consumes a following token only when StrictBooleanConverter accepts it so --flag maybe leaves maybe as a positional argument, and the positional error message is built with Collectors.joining passed a delimiter where a prefix belongs, so one stray value renders as ',maybe}'.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "BarclayValueModelDump",
          "golden": "crates/gatk-barclay/tests/data/value_model.txt.gz",
          "why": "From the pinned container on a real x86-64 runner, published as the golden-pending artefact of the run that added this suite. 368 rows over 41 command lines, no corrections. The pair that carries the claim is bounded-below against bounded-null: the same argument and the same bounds, reported as allowed range [1, 10] and as allowed range [1.0, 10.0], because the formatter asks whether the value is an Integer and a null is not."
        }
      ]
    },
    {
      "id": "barclay-tagged-arguments",
      "status": "oracle-backed",
      "title": "Barclay's tagged arguments and collection-file expansion",
      "harness": "tools/argument-conformance",
      "tools": [],
      "why": "G1.8's second slice, and the two mechanisms a covering-array vector reaches as soon as it names a logical name or a file. Tags are rewritten before jopt-simple sees the command line: the option name is peeled off, the pair of tag and raw value is stored under a surrogate key built from the option string and the value, and the parser is handed the bare option name and that key. The key doing double duty as a uniqueness test is what makes the same option with the same tag and the same value twice a duplicate on the command line rather than two values, while the same tag with two different values is fine. A tag on a field whose type does not implement TaggedArgument is refused when the value is set rather than when it is parsed, and the message is built from getShortName plus a slash plus getFullName with no guard, so an argument with no short name reports itself as slash plain-scalar. The malformed forms are four different exception shapes and two of them name no argument at all, leaving two spaces in the message. Expansion is collection-only: a collection value ending in .list or .args becomes the file's lines, trimmed, without blanks or hash comments, while the identical value on a scalar stays a path and suppressFileExpansion turns it off; a tag on a collection whose value is an expansion file is written onto every value the file produced.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "BarclayTaggedArgumentDump",
          "golden": "crates/gatk-barclay/tests/data/tagged_arguments.txt.gz",
          "why": "From the pinned container on a real x86-64 runner, published as the golden-pending artefact of the run that added this suite. 150 rows over 30 command lines, no corrections. The pair that carries the claim is same-option-tag-and-value-twice against same-tag-different-values: the surrogate key is built from the option string and the value, so the first collides with itself and the second does not."
        }
      ]
    },
    {
      "id": "barclay-argument-collections",
      "status": "oracle-backed",
      "title": "Barclay's @ArgumentCollection flattening",
      "harness": "tools/argument-conformance",
      "tools": [],
      "why": "G1.8's third slice, and the mechanism by which -L, -XL and the read-filter arguments actually reach a tool: they are not declared on the tool at all, they live on collection objects it holds, and createArgumentDefinitions recurses into those and adds their fields to one flat namespace where nothing records which object an argument came from. Two orderings are decided there and neither is stated anywhere. getAllFields adds a class's own declared fields and then climbs to its superclass, so a subclass's fields are registered before the ones it inherits, and that order is the order values are propagated in and the order a missing required argument is reported in: a user who omits both a subclass's required argument and its base class's is told about the subclass's. And the recursion is depth-first at the position of the field, so a collection declared between two arguments splices its own arguments between them rather than appending them. Two more refusals happen while the definitions are built rather than while a command line is parsed: an ArgumentCollection field left null, and a field carrying both annotations. A duplicate alias is a third, and it is a construction failure rather than a shadowing rule, so an outer object and a nested one declaring the same name make the tool unusable before it sees an argument.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "BarclayArgumentCollectionDump",
          "golden": "crates/gatk-barclay/tests/data/argument_collections.txt.gz",
          "why": "From the pinned container on a real x86-64 runner, published as the golden-pending artefact of the run that added this suite. 42 rows, no corrections. The rows that carry the claim are the eight defs rows: the subclass's own field is index 0, its nested collection occupies 1 to 4, and the base class's two fields are 6 and 7."
        }
      ]
    },
    {
      "id": "barclay-arguments-file",
      "status": "oracle-backed",
      "title": "Barclay's --arguments_file",
      "harness": "tools/argument-conformance",
      "tools": [],
      "why": "G1.8's fourth slice, and the only argument that changes the command line rather than a field. When the special arguments-file argument is present, parseArguments reads the named files, splits every non-comment non-blank line on runs of whitespace, and calls itself again on the result. The file's arguments come first, because newArgs.addAll(Arrays.asList(args)) appends the original command line to the expansion rather than the other way round, and it does so wherever the arguments-file argument itself sat: a collection given a value in a file and another on the command line ends up with the file's first either way, and a scalar given in both is cannot be specified more than once rather than an override. The recursion is bounded by a set of file names rather than a depth, and every file named in a pass goes into that set including ones skipped for already being there, so a file that includes itself is read once and two files that include each other are read once each. The argument is not removed from the command line between passes: the recursion terminates because the second expansion is empty, not because the argument is gone. And the tag surrogates are reset between passes, because the first pass built keys the second would build again. Splitting is StringUtils.split, which collapses runs of whitespace, so an argument value containing a space cannot be written in one of these files at all.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "BarclayArgumentsFileDump",
          "golden": "crates/gatk-barclay/tests/data/arguments_file.txt.gz",
          "why": "From the pinned container on a real x86-64 runner, published as the golden-pending artefact of the run that added this suite. 76 rows over 14 command lines, no corrections. The pair that carries the claim is file-then-command-line against command-line-then-file: the same three values in the same order both ways, because the expansion is what the original command line is appended to."
        }
      ]
    },
    {
      "id": "barclay-plugin-descriptors",
      "status": "oracle-backed",
      "title": "Barclay's plugin descriptors",
      "harness": "tools/argument-conformance",
      "tools": [],
      "why": "G1.8's fifth slice, measured through GATK's own GATKReadFilterPluginDescriptor over GATK's own read filters rather than a stand-in, because --read-filter is this mechanism. A descriptor names a base class and a package, ClassFinder finds every implementation, and each one's arguments are registered as controlled by that descriptor, so every read filter's arguments are in the parser on every run whether or not anybody asked for that filter. What decides whether one is usable is validatePluginArgumentValues, which runs before the required check and rewrites the list it checks: a controlled argument that nobody set and whose filter nobody named is removed from the list rather than treated as optional, so a required argument of an unselected filter would not fire at all. The same argument set without its filter is an error that names the filter class rather than --read-filter, and builds the argument's own name from the short name, a slash and the long name with no guard, so one with no short name reports a leading slash. And the refusal of a name matching no filter is the descriptor's own validateAndResolvePlugins rather than the argument layer, which is why its wording is GATK's.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "BarclayPluginDescriptorDump",
          "golden": "crates/gatk-barclay/tests/data/plugin_descriptors.txt.gz",
          "why": "From the pinned container on a real x86-64 runner, published as the golden-pending artefact of the run that added this suite. 48 rows over 11 command lines, no corrections. The rows that carry the claim are no-filter against dependent-without-filter: the same argument, left alone and set, with an unselected filter."
        }
      ]
    },
    {
      "id": "natural-log-utils",
      "status": "oracle-backed",
      "title": "NaturalLogUtils, and the rows that cannot depend on exp",
      "harness": "tools/annotation-conformance",
      "tools": [],
      "why": "G1.9.1, and the layer AllelePseudoDepth stands on. Compared as raw bit patterns, because the question is whether a port built on a permissively-licensed exp lands on the same double and decimal rendering discards exactly that. The reference calls Math.exp; the port calls FDLIBM, measured at one ulp worst case against it by htsjdk-rs decision 0025, so this suite is not a bit-identity claim and does not pretend to be. It separates three things. First, the rows that reach neither exp nor log and are therefore exact by construction: logSumExp starts its accumulator at one rather than zero because the maximum's own term is skipped in the loop and folded in as that one, and the guard sum not equal to one then skips the log entirely, so a single non-infinite entry, a maximum with everything else at negative infinity, and a difference large enough that one plus exp of it rounds back to one all return the maximum untouched. Second, the rows that do call exp, which must land within one ulp, and the test reports the worst distance rather than only passing. Third, the refusal, which fires on the accumulator after the loop rather than on the inputs, so a NaN input reaches it and a negative infinity input does not, though the message names the inputs either way. A row diverging by more than one ulp means the port's arithmetic is wrong rather than that the exponential is approximate, which is the distinction the suite is built to make.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "NaturalLogUtilsDump",
          "golden": "crates/gatk-engine/tests/data/natural_log_utils.txt.gz",
          "why": "From the pinned container on a real x86-64 runner, published as the golden-pending artefact of the run that added this suite. 42 rows, no corrections. It happens to be byte-identical to an Apple Silicon run, which says the two hosts' Math.exp agreed on these particular inputs and says nothing about the general case; the provenance is what makes it a golden."
        }
      ]
    },
    {
      "id": "somatic-likelihoods",
      "status": "oracle-backed",
      "title": "The Dirichlet fixed point, with its iteration count",
      "harness": "tools/annotation-conformance",
      "tools": [],
      "why": "G1.9.2. The variational fixed point AllelePseudoDepth ends in, and where G1.9's open question lives. G1.9.1 established that a one-ulp exp is what enters here; this measures what the iteration does with it, and there are two ways that could go wrong which look identical unless they are separated. Amplification, where the difference grows across iterations. And a different iteration count, because convergence is a threshold test on distance1 over sum against a thousandth, so a difference far too small to see in the values can still put one side of that comparison on the other side of the threshold, after which the two runs do a different amount of work. The second would be far larger than the first, so the golden carries the count and the suite asserts it exactly, before comparing any value. The count is not exposed by the engine, so the harness replays the loop body with the same calls in the same order and checks its replay against the engine's own answer on every case, which the agree rows record. The weights rows are the control: effectiveLogMultinomialWeights is digamma arithmetic with no exp in it, so they carry no bound and must be bit-identical.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "SomaticLikelihoodsDump",
          "golden": "crates/gatk-engine/tests/data/somatic_likelihoods.txt.gz",
          "why": "From the pinned container on a real x86-64 runner, published as the golden-pending artefact of the run that added this suite. 29 rows, no corrections. The result it carries is the one G1.9 needed: every iteration count matches and the worst value divergence is zero, so the fixed point neither amplifies the exp difference nor crosses its convergence threshold differently."
        }
      ]
    },
    {
      "id": "decimal-format",
      "status": "oracle-backed",
      "title": "DecimalFormat, for the two patterns AllelePseudoDepth emits through",
      "harness": "tools/annotation-conformance",
      "tools": [],
      "why": "G1.9.3. AllelePseudoDepth writes strings, not numbers, so java.text.DecimalFormat is part of its output. The Javadoc specifies HALF_EVEN and the pattern language and says nothing about the three facts that decide printed digits, each established here by measurement rather than by reading the class, which is GPL2 and may not be transcribed. First, it rounds the SHORTEST decimal form and not the value: formatting 0.1 to forty places gives 0.1 rather than the double's true 0.1000000000000000055511151231257827. Second, an apparent tie is settled by which side of the halfway point the double sits on, so 0.155 rounds down and 0.165 rounds up although the digit strings are the same shape, and only when the shortest form IS the value is it a real tie for HALF_EVEN to break on parity. Third, the two patterns disagree with each other at one place: where the rounding position falls before the first digit, #.## follows the value and #.#### rounds to even, so 0.005 and 5e-5 are the same shape one decade apart and go opposite ways. Values are bit patterns because a decimal literal in a golden would be re-parsed into a possibly different double, which for a suite about the last digit is the whole question.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "DecimalFormatDump",
          "golden": "crates/gatk-annotation/tests/data/decimal_format.txt.gz",
          "why": "From the pinned container on a real x86-64 runner, published as the golden-pending artefact of the run that added this suite. 315 rows, no corrections, and byte-identical to the same container on Apple Silicon: nothing the formatter does here depends on the host. 630 strings compared and two values diverge, both above 2^53, where Java 17's pre-Schubfach digit generation stops producing the shortest decimal form. Below that line there is no divergence at all, measured on 903,121 values."
        }
      ]
    },
    {
      "id": "allele-pseudo-depth",
      "status": "oracle-backed",
      "title": "DD and DF, the annotation G1.9 was opened for",
      "harness": "tools/annotation-conformance",
      "tools": [],
      "why": "G1.9.3, and the end of G1.9. AllelePseudoDepth emits its two keys as STRINGS through DecimalFormat at two and four decimals, which is what makes an oracle-backed suite possible at all for a chain that calls Math.exp: G1.9.1 measured the port within 1 ulp of the reference and G1.9.2 measured the fixed point at zero ulp, and the formatter is about twelve orders of magnitude coarser than a last bit. The suite therefore compares the formatted strings, which is the only thing the reference writes. Four behaviours are the reason for the case list, and none of them is the arithmetic. composePriorPseudoCounts memoises one array per allele count and hands out that array rather than a copy, so on the empty-evidence branch the posteriors ARE the memo and the closing subtraction zeroes it; every case is annotated twice with the same object, and the reference's own second answer for empty-evidence is NaN,NaN. The log10 branch's visitor receives (row, column) as (allele, read) and looks the mapping quality up at evidence().get(row), so a site with more alleles than reads throws IndexOutOfBoundsException and one with fewer floors each allele's row using an unrelated read's quality. The decay rate selects which intrinsics run: one Math.pow per read at the default, two when it is neither zero nor one, and none at zero, where the weights are null. And the guards put no key in the genotype at all, which is not the same as an empty one. The matrices are graded rather than symmetric, because a symmetric site gives every read the same weight and every setting of weightDecay the same answer.",
      "compare": {
        "mode": "lines"
      },
      "cases": [
        {
          "dump": "AllelePseudoDepthDump",
          "golden": "crates/gatk-annotation/tests/data/allele_pseudo_depth.txt.gz",
          "why": "From the pinned container on a real x86-64 runner, published as the golden-pending artefact of the run that added this suite. 69 rows, no corrections, and byte-identical to the same container on Apple Silicon. All 46 calls match, including the two whose second answer differs from their first because the reference wrote through its own memo."
        }
      ]
    }
  ],
  "probes": []
}
