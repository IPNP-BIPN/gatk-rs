name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  guard:
    name: Repository invariants
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: LICENSE and README state the same licence
        # gatk-rs is Apache 2.0 because GATK is Apache 2.0; htsjdk-rs and picard-rs are MIT.
        # Getting these crossed is easy and consequential, so it is checked rather than remembered.
        # Several third-party descriptions call GATK BSD-3-Clause; the authority is LICENSE.TXT in
        # the pinned clone.
        run: |
          grep -q "Apache License" LICENSE || { echo "LICENSE is not Apache 2.0"; exit 1; }
          grep -q "Apache License 2.0, matching GATK" README.md || { echo "README licence claim changed"; exit 1; }
          grep -qi "not the official GATK" README.md || { echo "attribution notice missing"; exit 1; }
          echo "licence and attribution consistent"

      - name: The workflow matches the conformance manifest
        # The oracle jobs are generated from tools/conformance/manifest.json. picard-rs's hand
        # edits grew that file to 1556 lines of drifted comparators before this guard existed.
        run: python3 tools/conformance/generate_ci.py --check

      - name: Every committed golden belongs to a declared suite
        # A golden nothing re-derives can only ever be compared against itself; picard-rs decision
        # 0008 records what that cost there.
        run: python3 tools/conformance/audit_goldens.py

  test:
    name: Test on real x86-64
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: nasm, which is what makes GKL's igzip exact rather than merely valid
        # `gkl-igzip` links ISA-L, and ISA-L without an assembler falls back to its readable C.
        # That C still returns valid deflate; it is simply not GKL's, and `gkl-deflate` refuses the
        # levels it would have answered rather than guessing. GATKConfig writes at level TWO, which
        # is one of them, so a build without nasm cannot write a block-compressed file at all.
        run: sudo apt-get update -qq && sudo apt-get install -y -qq nasm && nasm -v

      - uses: dtolnay/rust-toolchain@master
        with:
          # Pinned; must match rust-toolchain.toml.
          toolchain: "1.97.1"
          components: rustfmt, clippy

      - uses: Swatinem/rust-cache@v2

      - name: Every ported symbol comes from a licence-compatible source
        # htsjdk-rs decision 0014: a transcription of GPL2-only HotSpot source was published in an
        # MIT crate and found eleven commits later by reading module headers. The same guard runs
        # in all three repositories now, not just the one where it was learned.
        run: python3 tools/audit/provenance.py crates

      - name: A file already explained does not lose its explanations
        # docs/COMMENTING.md. The guard is a ratchet, not a floor for the whole tree: it fails only
        # when a file on tools/audit/commented.txt drops below the density it was recorded at, so
        # the remaining files are a measured list rather than a feeling.
        run: python3 tools/audit/comment_density.py --check

      - run: cargo fmt --all -- --check
      - run: cargo clippy --workspace --all-targets -- -D warnings
      - run: cargo test --workspace --release

      - name: The ports below are pinned by revision, and every revision resolves
        # GATK's build.gradle pins picardVersion and htsjdkVersion. A floating dependency would let
        # the layer underneath change under a byte-identity claim without anything failing.
        #
        # Checking the FORM of the pin is not enough. `picard-analysis` carried htsjdk-rs's SHA on a
        # picard-rs URL for months and nothing noticed, because no crate depended on it: cargo never
        # resolved the pin, so it never had to exist. Every revision named in Cargo.toml must also
        # appear in Cargo.lock, which is cargo's own record of having resolved it.
        run: |
          if grep -E '^(htsjdk|picard|jmath)[a-z-]* = \{ git' Cargo.toml | grep -qv 'rev = "'; then
            echo "a dependency is not pinned to a revision"
            exit 1
          fi
          revisions=$(grep -oE 'rev = "[0-9a-f]+"' Cargo.toml | grep -oE '[0-9a-f]+' | sort -u)
          for revision in $revisions; do
            if ! grep -q "$revision" Cargo.lock; then
              echo "revision $revision is pinned in Cargo.toml and resolved nowhere in Cargo.lock"
              exit 1
            fi
          done
          echo "$revisions" | wc -l

  determinism:
    name: "Determinism: the same suites in a different environment"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: nasm, which is what makes GKL's igzip exact rather than merely valid
        # `gkl-igzip` links ISA-L, and ISA-L without an assembler falls back to its readable C.
        # That C still returns valid deflate; it is simply not GKL's, and `gkl-deflate` refuses the
        # levels it would have answered rather than guessing. GATKConfig writes at level TWO, which
        # is one of them, so a build without nasm cannot write a block-compressed file at all.
        run: sudo apt-get update -qq && sudo apt-get install -y -qq nasm && nasm -v

      - uses: dtolnay/rust-toolchain@master
        with:
          # Pinned; must match rust-toolchain.toml.
          toolchain: "1.97.1"

      - uses: Swatinem/rust-cache@v2

      - name: The suites pass under a foreign locale, timezone, temporary directory and one thread
        # V.7. Every claim in this repository is a byte claim, and a byte claim that holds only in
        # one environment is not one. The `test` job already runs the suites in the runner's own
        # environment; this runs them in a deliberately different one.
        #
        # LC_ALL is the axis that has actually bitten this programme: htsjdk-rs decision 0011
        # established that the REFERENCE's metrics formatting is locale-dependent, and the oracle
        # pins en_US because of it. Rust's `format!` is locale-independent by construction, so this
        # gate is what turns "by construction" into "measured": under fr_FR a locale-sensitive
        # formatter writes `0,333333` and every suite comparing a formatted number would fail.
        #
        # TZ moves any accidental date formatting; TMPDIR moves every fixture the suites write, so
        # a path that leaked into an output would show; and one test thread removes the scheduling
        # that hid a fixture race until it reached a loaded runner.
        run: |
          set -eux
          mkdir -p /tmp/gatk-rs-elsewhere
          sudo locale-gen fr_FR.UTF-8 || true
          LC_ALL=fr_FR.UTF-8 LANG=fr_FR.UTF-8 TZ=Asia/Kolkata \
            TMPDIR=/tmp/gatk-rs-elsewhere RUST_TEST_THREADS=1 \
            cargo test --workspace --release

      - name: Two clean builds of the same source are the same bytes
        # A port whose own build is not reproducible cannot support a claim that its output is.
        # One crate is enough to catch a build that embeds a path, a timestamp or a hostname, and
        # `gatk-engine` is the one every tool depends on.
        run: |
          set -eux
          cargo build --release -p gatk-engine
          find target/release -maxdepth 1 -name 'libgatk_engine*.rlib' -exec sha256sum {} \; \
            | sed 's#.*/##' > /tmp/build1.txt
          cargo clean
          cargo build --release -p gatk-engine
          find target/release -maxdepth 1 -name 'libgatk_engine*.rlib' -exec sha256sum {} \; \
            | sed 's#.*/##' > /tmp/build2.txt
          diff /tmp/build1.txt /tmp/build2.txt
          cat /tmp/build1.txt

# The jobs below this line are generated from tools/conformance/manifest.json by
# tools/conformance/generate_ci.py. Edit the manifest, not this file: the `guard` job
# fails if the two disagree.
  oracle-image:
    name: Build the pinned oracle image
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Record the host CPU
        run: grep -m1 'model name' /proc/cpuinfo; grep -m1 'vendor_id' /proc/cpuinfo

      - id: cache
        uses: actions/cache@v6
        with:
          path: /tmp/oracle-image.tar
          key: gatk-rs-oracle-${{ hashFiles('tools/oracle/**') }}

      - name: Build the oracle image
        # The probe runs during the build, so a degraded environment cannot produce an image, let
        # alone a golden. The image is built once and shared with every suite below, because
        # rebuilding it per suite would multiply a five-minute build by the number of suites.
        if: steps.cache.outputs.cache-hit != 'true'
        run: |
          docker build --platform linux/amd64 -t gatk-rs-oracle:4.6.2.0 tools/oracle
          docker save gatk-rs-oracle:4.6.2.0 -o /tmp/oracle-image.tar

  oracle:
    name: "oracle ${{ matrix.group }} ${{ matrix.index }}"
    needs: oracle-image
    runs-on: ubuntu-latest
    strategy:
      # One suite's divergence must not cancel the others: the point of the run is to learn which
      # suites diverge, not to learn that at least one does.
      fail-fast: false
      matrix:
        include:
          - group: oracle-backed
            index: "1/60"
            slug: "annotation-original-alignment-annotation-per-allele-variant-getters-annotation-likelihood-counts-genotyper-allele-likelihoods"
            suites: "annotation-original-alignment annotation-per-allele variant-getters annotation-likelihood-counts genotyper-allele-likelihoods"
          - group: oracle-backed
            index: "2/60"
            slug: "genotyper-allele-list-annotation-counting-readfilters-readsdatasource-referencecontext"
            suites: "genotyper-allele-list annotation-counting readfilters readsdatasource referencecontext"
          - group: oracle-backed
            index: "3/60"
            slug: "reference-walker-count-reads-and-bases-fasta-reference-maker-shift-fasta-fasta-alternate-reference-maker"
            suites: "reference-walker count-reads-and-bases fasta-reference-maker shift-fasta fasta-alternate-reference-maker"
          - group: oracle-backed
            index: "4/60"
            slug: "get-pileup-summaries-pileup-tool-ase-read-counter-collect-allelic-counts-normal-artifact-data"
            suites: "get-pileup-summaries pileup-tool ase-read-counter collect-allelic-counts normal-artifact-data"
          - group: oracle-backed
            index: "5/60"
            slug: "call-copy-ratio-segments-annotate-intervals-collect-read-counts-denoise-read-counts-filter-intervals"
            suites: "call-copy-ratio-segments annotate-intervals collect-read-counts denoise-read-counts filter-intervals"
          - group: oracle-backed
            index: "6/60"
            slug: "readwalker-printreads-record-transform-fix-misencoded-add-oa-tags"
            suites: "readwalker printreads record-transform fix-misencoded add-oa-tags"
          - group: oracle-backed
            index: "7/60"
            slug: "print-distant-mates-split-reads-left-align-indels-left-align-indels-tool-clip-reads"
            suites: "print-distant-mates split-reads left-align-indels left-align-indels-tool clip-reads"
          - group: oracle-backed
            index: "8/60"
            slug: "transfer-read-tags-print-reads-header-post-process-reads-for-rsem-convert-headerless-shard-gatk-report"
            suites: "transfer-read-tags print-reads-header post-process-reads-for-rsem convert-headerless-shard gatk-report"
          - group: oracle-backed
            index: "9/60"
            slug: "recal-datum-covariates-qual-quantizer-recalibration-tables-gatk-report-reader"
            suites: "recal-datum covariates qual-quantizer recalibration-tables gatk-report-reader"
          - group: oracle-backed
            index: "10/60"
            slug: "bqsr-transformer-recalibration-report-apply-bqsr-baq-base-recalibration-engine"
            suites: "bqsr-transformer recalibration-report apply-bqsr baq base-recalibration-engine"
          - group: oracle-backed
            index: "11/60"
            slug: "base-recalibrator-read-anonymizer-intervalwalker-alignmentstate-pileupelement"
            suites: "base-recalibrator read-anonymizer intervalwalker alignmentstate pileupelement"
          - group: oracle-backed
            index: "12/60"
            slug: "intervalfile-jexlfilter-featurecache-readpileup-readstates"
            suites: "intervalfile jexlfilter featurecache readpileup readstates"
          - group: oracle-backed
            index: "13/60"
            slug: "locusiterator-acbuilder-locuswalker-javarandom-reservoir"
            suites: "locusiterator acbuilder locuswalker javarandom reservoir"
          - group: oracle-backed
            index: "14/60"
            slug: "well19937c-leveling-variantwalker-activityprofile-assemblyregion"
            suites: "well19937c leveling variantwalker activityprofile assemblyregion"
          - group: oracle-backed
            index: "15/60"
            slug: "intervaliterators-assemblyregioniterator-assemblyregionwalker-mann-whitney-annotation-rank-sum"
            suites: "intervaliterators assemblyregioniterator assemblyregionwalker mann-whitney annotation-rank-sum"
          - group: oracle-backed
            index: "16/60"
            slug: "annotation-strand-bias-annotation-read-grouping-annotation-depth-per-allele-annotation-flow-annotation-site-statistics"
            suites: "annotation-strand-bias annotation-read-grouping annotation-depth-per-allele annotation-flow annotation-site-statistics"
          - group: oracle-backed
            index: "17/60"
            slug: "annotation-heterozygosity-and-mq-annotation-allele-specific-rank-sum-annotation-allele-specific-strand-bias-annotation-allele-specific-site-statistics-annotation-pedigree"
            suites: "annotation-heterozygosity-and-mq annotation-allele-specific-rank-sum annotation-allele-specific-strand-bias annotation-allele-specific-site-statistics annotation-pedigree"
          - group: oracle-backed
            index: "18/60"
            slug: "annotation-fragment-counts-annotation-haplotype-filtering-multipasswalker-barclay-value-model-barclay-tagged-arguments"
            suites: "annotation-fragment-counts annotation-haplotype-filtering multipasswalker barclay-value-model barclay-tagged-arguments"
          - group: oracle-backed
            index: "19/60"
            slug: "barclay-argument-collections-barclay-arguments-file-barclay-plugin-descriptors-natural-log-utils-somatic-likelihoods"
            suites: "barclay-argument-collections barclay-arguments-file barclay-plugin-descriptors natural-log-utils somatic-likelihoods"
          - group: oracle-backed
            index: "20/60"
            slug: "decimal-format-string-format-double-to-string-allele-pseudo-depth-counting-walkers"
            suites: "decimal-format string-format double-to-string allele-pseudo-depth counting-walkers"
          - group: oracle-backed
            index: "21/60"
            slug: "sa-tag-builder-overhang-fixing-manager-split-n-cigar-reads-methylation-type-caller-get-sample-name"
            suites: "sa-tag-builder overhang-fixing-manager split-n-cigar-reads methylation-type-caller get-sample-name"
          - group: oracle-backed
            index: "22/60"
            slug: "compare-base-qualities-sam-pileup-codec-check-pileup-dump-tabix-index-tsv-table"
            suites: "compare-base-qualities sam-pileup-codec check-pileup dump-tabix-index tsv-table"
          - group: oracle-backed
            index: "23/60"
            slug: "pileup-summary-contamination-tables-persistence-optimizer-analyze-covariates-remove-nearby-indels"
            suites: "pileup-summary contamination-tables persistence-optimizer analyze-covariates remove-nearby-indels"
          - group: oracle-backed
            index: "24/60"
            slug: "update-vcf-sequence-dictionary-left-align-and-trim-trim-alleles-split-biallelics-left-align-and-trim-variants"
            suites: "update-vcf-sequence-dictionary left-align-and-trim trim-alleles split-biallelics left-align-and-trim-variants"
          - group: oracle-backed
            index: "25/60"
            slug: "genotype-index-subset-alleles-split-with-likelihoods-variant-filtration-gather-vcfs"
            suites: "genotype-index subset-alleles split-with-likelihoods variant-filtration gather-vcfs"
          - group: oracle-backed
            index: "26/60"
            slug: "select-variants-samples-java-regex-select-variants-subset-select-variants-filters-select-variants-output"
            suites: "select-variants-samples java-regex select-variants-subset select-variants-filters select-variants-output"
          - group: oracle-backed
            index: "27/60"
            slug: "select-variants-header-select-variants-concordance-variants-to-table-validate-variants-count-variants"
            suites: "select-variants-header select-variants-concordance variants-to-table validate-variants count-variants"
          - group: oracle-backed
            index: "28/60"
            slug: "count-false-positives-calculate-mixing-fractions-annotate-vcf-with-bam-depth-annotate-vcf-with-expected-allele-fraction-concordance-walker"
            suites: "count-false-positives calculate-mixing-fractions annotate-vcf-with-bam-depth annotate-vcf-with-expected-allele-fraction concordance-walker"
          - group: oracle-backed
            index: "29/60"
            slug: "evaluate-info-field-concordance-concordance-summary-concordance-filter-analysis-concordance-annotated-vcfs-apply-vqsr-tranches"
            suites: "evaluate-info-field-concordance concordance-summary concordance-filter-analysis concordance-annotated-vcfs apply-vqsr-tranches"
          - group: oracle-backed
            index: "30/60"
            slug: "apply-vqsr-site-filtering-apply-vqsr-allele-specific-apply-vqsr-two-modes-filter-variant-tranches-threshold-calculator"
            suites: "apply-vqsr-site-filtering apply-vqsr-allele-specific apply-vqsr-two-modes filter-variant-tranches threshold-calculator"
          - group: oracle-backed
            index: "31/60"
            slug: "filter-stats-mutect-hard-filters-mutect-engine-arithmetic-germline-probability-mutect-engine-construction"
            suites: "filter-stats mutect-hard-filters mutect-engine-arithmetic germline-probability mutect-engine-construction"
          - group: oracle-backed
            index: "32/60"
            slug: "mutect-error-probabilities-beta-binomial-somatic-validation-power-allele-fraction-cluster-somatic-clustering-model"
            suites: "mutect-error-probabilities beta-binomial somatic-validation-power allele-fraction-cluster somatic-clustering-model"
          - group: oracle-backed
            index: "33/60"
            slug: "somatic-clustering-learn-mutect-filter-list-filter-mutect-calls-allele-filter-base-normal-artifact-filter"
            suites: "somatic-clustering-learn mutect-filter-list filter-mutect-calls allele-filter-base normal-artifact-filter"
          - group: oracle-backed
            index: "34/60"
            slug: "remaining-hard-filters-slippage-filter-contamination-filter-haplotype-filter-strand-artifact-estep"
            suites: "remaining-hard-filters slippage-filter contamination-filter haplotype-filter strand-artifact-estep"
          - group: oracle-backed
            index: "35/60"
            slug: "strand-artifact-mstep-filter-list-by-mode-apply-filters-accumulate-data-engine-assembly"
            suites: "strand-artifact-mstep filter-list-by-mode apply-filters accumulate-data engine-assembly"
          - group: oracle-backed
            index: "36/60"
            slug: "germline-wrapper-pair-hmm-kernel-segmentation-contamination-model-allele-pileup-counter"
            suites: "germline-wrapper pair-hmm kernel-segmentation contamination-model allele-pileup-counter"
          - group: oracle-backed
            index: "37/60"
            slug: "validate-basic-somatic-short-mutations-preprocess-intervals-interval-list-scatter-split-intervals-compare-interval-lists"
            suites: "validate-basic-somatic-short-mutations preprocess-intervals interval-list-scatter split-intervals compare-interval-lists"
          - group: oracle-backed
            index: "38/60"
            slug: "callable-loci-merge-annotated-regions-tag-germline-events-merge-annotated-regions-by-annotation-combine-segment-breakpoints"
            suites: "callable-loci merge-annotated-regions tag-germline-events merge-annotated-regions-by-annotation combine-segment-breakpoints"
          - group: oracle-backed
            index: "39/60"
            slug: "mutect-gathers-gather-bqsr-reports-gather-tranches-check-terminator-block-rename-sample-in-vcf"
            suites: "mutect-gathers gather-bqsr-reports gather-tranches check-terminator-block rename-sample-in-vcf"
          - group: oracle-backed
            index: "40/60"
            slug: "make-sites-only-vcf-split-vcfs-sort-vcf-merge-vcfs-fix-vcf-header"
            suites: "make-sites-only-vcf split-vcfs sort-vcf merge-vcfs fix-vcf-header"
          - group: oracle-backed
            index: "41/60"
            slug: "filter-vcf-picard-update-vcf-dictionary-vcf-to-interval-list-picard-gather-vcfs-vcf-format-converter"
            suites: "filter-vcf picard-update-vcf-dictionary vcf-to-interval-list picard-gather-vcfs vcf-format-converter"
          - group: oracle-backed
            index: "42/60"
            slug: "build-bam-index-print-bgzf-block-information-index-feature-file-set-nm-md-uq-tags-gather-bam-files"
            suites: "build-bam-index print-bgzf-block-information index-feature-file set-nm-md-uq-tags gather-bam-files"
          - group: oracle-backed
            index: "43/60"
            slug: "add-comments-to-bam-reference-block-concordance-calculate-average-combined-annotations-print-file-diagnostics-compare-references"
            suites: "add-comments-to-bam reference-block-concordance calculate-average-combined-annotations print-file-diagnostics compare-references"
          - group: oracle-backed
            index: "44/60"
            slug: "check-reference-compatibility-condense-depth-evidence-site-depth-to-baf-multi-feature-walker-numt-filter"
            suites: "check-reference-compatibility condense-depth-evidence site-depth-to-baf multi-feature-walker numt-filter"
          - group: oracle-backed
            index: "45/60"
            slug: "mt-low-heteroplasmy-merge-mutect2-mc3-downsample-by-duplicate-set-print-sv-evidence-print-read-counts"
            suites: "mt-low-heteroplasmy merge-mutect2-mc3 downsample-by-duplicate-set print-sv-evidence print-read-counts"
          - group: oracle-backed
            index: "46/60"
            slug: "split-cram-collect-f1r2-counts-filter-funcotations-pathseq-build-reference-taxonomy-pathseq-build-kmers"
            suites: "split-cram collect-f1r2-counts filter-funcotations pathseq-build-reference-taxonomy pathseq-build-kmers"
          - group: oracle-backed
            index: "47/60"
            slug: "gtf-to-bed-compose-str-table-file-cram-issue-8768-detector-add-flow-base-quality-gene-expression-evaluation"
            suites: "gtf-to-bed compose-str-table-file cram-issue-8768-detector add-flow-base-quality gene-expression-evaluation"
          - group: oracle-backed
            index: "48/60"
            slug: "funcotator-data-source-downloader-sv-stratify-add-flow-snv-quality-calculate-genotype-posteriors-family-priors"
            suites: "funcotator-data-source-downloader sv-stratify add-flow-snv-quality calculate-genotype-posteriors family-priors"
          - group: oracle-backed
            index: "49/60"
            slug: "sv-cluster-grouped-sv-cluster-sv-concordance-sv-annotate-joint-germline-cnv-segmentation"
            suites: "sv-cluster grouped-sv-cluster sv-concordance sv-annotate joint-germline-cnv-segmentation"
          - group: oracle-backed
            index: "50/60"
            slug: "collect-sv-evidence-filter-alignment-artifacts-learn-read-orientation-model-brent-optimizer-create-somatic-panel-of-normals"
            suites: "collect-sv-evidence filter-alignment-artifacts learn-read-orientation-model brent-optimizer create-somatic-panel-of-normals"
          - group: oracle-backed
            index: "51/60"
            slug: "ramped-haplotype-caller-series-stats-depth-of-coverage-vcf-comparator-variant-annotator"
            suites: "ramped-haplotype-caller series-stats depth-of-coverage vcf-comparator variant-annotator"
          - group: oracle-backed
            index: "52/60"
            slug: "reblock-gvcf-combine-gvcfs-variant-eval-genotype-gvcfs-structural-variant-discoverer"
            suites: "reblock-gvcf combine-gvcfs variant-eval genotype-gvcfs structural-variant-discoverer"
          - group: oracle-backed
            index: "53/60"
            slug: "funcotate-segments-extract-variant-annotations-variant-recalibrator-haplotype-based-variant-recaller-flow-feature-mapper"
            suites: "funcotate-segments extract-variant-annotations variant-recalibrator haplotype-based-variant-recaller flow-feature-mapper"
          - group: oracle-backed
            index: "54/60"
            slug: "flow-pairhmm-align-reads-to-haplotypes-calibrate-dragstr-model-analyze-saturation-mutagenesis-create-read-count-panel-of-normals-ground-truth-reads-builder"
            suites: "flow-pairhmm-align-reads-to-haplotypes calibrate-dragstr-model analyze-saturation-mutagenesis create-read-count-panel-of-normals ground-truth-reads-builder"
          - group: oracle-backed
            index: "55/60"
            slug: "ground-truth-scorer-gnarly-genotyper-model-segments-local-assembler-funcotator"
            suites: "ground-truth-scorer gnarly-genotyper model-segments local-assembler funcotator"
          - group: oracle-backed
            index: "56/60"
            slug: "main-dispatch-main-catalogue-gatk-config-main-usage-sequence-dictionary-validation"
            suites: "main-dispatch main-catalogue gatk-config main-usage sequence-dictionary-validation"
          - group: oracle-backed
            index: "57/60"
            slug: "interval-arguments-filter-resolution-expanded-command-line-main-non-user-main-entry"
            suites: "interval-arguments filter-resolution expanded-command-line main-non-user main-entry"
          - group: oracle-backed
            index: "58/60"
            slug: "splitting-index-allele-frequency-qc-calculate-contamination-usage-text-bwa-index-image"
            suites: "splitting-index allele-frequency-qc calculate-contamination usage-text bwa-index-image"
          - group: oracle-backed
            index: "59/60"
            slug: "tool-argument-value-classes-plugin-argument-ownership-read-filter-catalogue-mutex-target-names-count-reads-plumbing"
            suites: "tool-argument-value-classes plugin-argument-ownership read-filter-catalogue mutex-target-names count-reads-plumbing"
          - group: oracle-backed
            index: "60/60"
            slug: "read-walker-refusals"
            suites: "read-walker-refusals"
          - group: golden-pending
            index: "1/1"
            slug: "tool-argument-declarations-tool-argument-enums"
            suites: "tool-argument-declarations tool-argument-enums"
    steps:
      - uses: actions/checkout@v7

      - uses: actions/cache/restore@v6
        with:
          path: /tmp/oracle-image.tar
          key: gatk-rs-oracle-${{ hashFiles('tools/oracle/**') }}
          fail-on-cache-miss: true

      - run: docker load -i /tmp/oracle-image.tar

      - name: Regenerate and compare
        # Every rule that canonicalizes anything away is declared in tools/conformance/manifest.json
        # with the reason it is there; tools/conformance/compare.py is the only code that applies
        # them. Run the same thing locally with:
        #   python3 tools/conformance/run_suite.py --suites "${{ matrix.suites }}"
        run: python3 tools/conformance/run_suite.py --suites "${{ matrix.suites }}"

      - name: Publish what the container produced
        # Two cases, one artefact. A `golden-pending` suite has no golden to compare against and
        # one may not be committed from a developer machine (decision 0008). An `oracle-backed`
        # suite whose dump has grown a case needs its golden refreshed, and that refresh has to
        # come from here for the same reason. Both land in tools/conformance/pending/, and
        # committing the file is what makes the claim.
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: candidate-goldens-${{ matrix.slug }}
          path: tools/conformance/pending/
          if-no-files-found: ignore


  argument-coverage:
    name: The covering array, against the port binary
    needs: oracle-image
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/cache/restore@v6
        with:
          path: /tmp/oracle-image.tar
          key: gatk-rs-oracle-${{ hashFiles('tools/oracle/**') }}
          fail-on-cache-miss: true

      - run: docker load -i /tmp/oracle-image.tar

      - name: nasm, which is what makes GKL's igzip exact rather than merely valid
        # `gkl-igzip` links ISA-L, and ISA-L without an assembler falls back to its readable C.
        # That C still returns valid deflate; it is simply not GKL's, and `gkl-deflate` refuses the
        # levels it would have answered rather than guessing. GATKConfig writes at level TWO, which
        # is one of them, so a build without nasm cannot write a block-compressed file at all.
        run: sudo apt-get update -qq && sudo apt-get install -y -qq nasm && nasm -v

      - name: Build the port binary
        # linux/amd64, which is what the container runs. The port is executed INSIDE the container
        # rather than on the host, and the reason is the output: a Tribble index records the file
        # it was built from by path and by last-modified time, so a port run at a different mount
        # point produces a different index for a reason that is not the port's.
        run: cargo build --release -p gatk-cli

      - name: Run the array against both sides
        run: |
          python3 tools/coverage/measure.py \
            --tool IndexFeatureFile --tool PrintBGZFBlockInformation --tool CountReads \
            --tool CountVariants --tool CreateHadoopBamSplittingIndex \
            --tool PrintReads --tool GatherVcfsCloud --tool ApplyBQSR \
            --tool CountBases --tool FlagStat \
            --tool CountBasesInReference \
            --tool SplitIntervals \
            --tool Pileup --tool PreprocessIntervals \
            --tool GetSampleName --tool PrintDistantMates \
            --tool CompareIntervalLists --tool FixMisencodedBaseQualityReads \
            --port target/release/gatk-rs --corpus-dir /tmp/coverage-corpus

      - name: The committed measurement is the one this run produces
        # Same rule as a golden: the number in the tree has to be the number CI re-derives, or the
        # dashboard is printing a claim nothing checks. A first run publishes the file as an
        # artefact and this step reports the diff rather than failing, which is how the first
        # measurement gets into the tree at all.
        run: git --no-pager diff --stat tools/coverage/measured.json

      - name: Publish what the run measured
        # The corpus goes with the number, and not as decoration: a share of 5/6 says a row
        # diverged and says nothing about which, and the corpus is where that is read.
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: candidate-coverage
          path: |
            tools/coverage/measured.json
            /tmp/coverage-corpus/
          if-no-files-found: ignore

  inventory:
    name: Inventory is internally consistent
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Check the generated inventory against itself
        # The inventory is generated, so the risk is not that it is wrong by hand but that it
        # is stale or partially regenerated. These checks catch both.
        run: |
          python3 - <<'PY'
          import json, re, sys
          inv = json.load(open('tools/inventory/generated/inventory.json'))
          fail = []

          counts = inv['counts']
          tools = inv['tools']

          if len(tools) != counts['tools']:
              fail.append(f"counts.tools={counts['tools']} but {len(tools)} tool entries")
          if counts['tools'] != counts.get('cli_reported_tools'):
              fail.append(f"tools={counts['tools']} != cli_reported_tools="
                          f"{counts.get('cli_reported_tools')}; the inventory no longer matches "
                          f"what `gatk --list` reports")

          total_args = sum(len(t['arguments']) for t in tools)
          if total_args != counts['arguments']:
              fail.append(f"counts.arguments={counts['arguments']} but {total_args} counted")

          # Every runnable tool must have at least one argument. A zero-argument entry means a
          # non-tool (a metrics definition class) leaked back into the tool list.
          empty = [t['name'] for t in tools if not t['arguments']]
          if empty:
              fail.append(f"{len(empty)} tools with no arguments, e.g. {empty[:5]}")

          # Names must be invocable: the " (Picard)" display suffix must have been stripped.
          suffixed = [t['name'] for t in tools if '(' in t['name']]
          if suffixed:
              fail.append(f"non-invocable tool names present: {suffixed[:5]}")

          # Branch names must be unique, or two tools would collide on one branch.
          branches = [t['branch'] for t in tools]
          dupes = {b for b in branches if branches.count(b) > 1}
          if dupes:
              fail.append(f"duplicate branch names: {sorted(dupes)[:5]}")

          for f in fail:
              print("FAIL:", f)
          print(f"tools={len(tools)} arguments={total_args} "
                f"spark={sum(1 for t in tools if t['spark'])} "
                f"undocumented={sum(1 for t in tools if not t.get('documented', True))}")
          sys.exit(1 if fail else 0)
          PY

      - name: Generator runs and is deterministic
        run: |
          python3 -c "import ast,sys; ast.parse(open('tools/inventory/generate_inventory.py').read())"
          python3 -c "import ast,sys; ast.parse(open('tools/inventory/extract_undocumented.py').read())"
          echo "generators parse"

  dashboard:
    name: The status dashboard matches its sources
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: docs/STATUS.md is generated, not written
        # It is generated from the inventory and the vendored port summaries under
        # tools/dashboard/ports, both of which are committed, so this runs with only this
        # repository checked out. Refreshing the summaries from the sibling working copies is
        # `--refresh`, and is a deliberate, reviewable commit.
        run: python3 tools/dashboard/generate.py --check

  coverage:
    name: Covering arrays cover what they claim
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: The arrays are verified, not asserted
        # --verify enumerates every t-way tuple of the domain and checks the array covers it, so a
        # regression in the generator fails here rather than producing an array that looks covered.
        # One small tool, one large one, and t=3 on the large one, which is the shape the plan
        # commits to on the critical path.
        run: |
          python3 tools/coverage/covering.py --tool CollectQualityYieldMetrics --t 2 --verify
          python3 tools/coverage/covering.py --tool HaplotypeCaller --t 2 --verify
          python3 tools/coverage/covering.py --tool HaplotypeCaller --t 3 --verify
          python3 tools/coverage/covering.py --tool HaplotypeCaller --t 2 --numeric-policy perturb --verify

      - name: Constraints actually forbid something
        # A constraint that quietly does nothing is indistinguishable from one that works, and the
        # failure mode is silent: the array still verifies, and every row it produces is rejected
        # by the reference at run time (picard-rs decision 0009). So CI asserts that the forbidden
        # count is non-zero and that the array still covers every permitted tuple.
        run: |
          python3 tools/coverage/covering.py --tool CollectQualityYieldMetrics --t 2 \
            --fixtures tools/coverage/constraints.example.json --verify | tee /tmp/c.txt
          grep -q "missing=0" /tmp/c.txt || { echo "constrained array does not cover its space"; exit 1; }
          grep -qE "forbidden by constraints=[1-9]" /tmp/c.txt || {
            echo "the constraint forbade nothing; it is not being applied"
            exit 1
          }

      - name: The generator is deterministic
        # The arrays are meant to be regenerated and diffed. Two runs that disagree would make
        # every diff meaningless, and IPOG has enough tie-breaks for that to happen by accident.
        run: |
          python3 tools/coverage/covering.py --tool HaplotypeCaller --t 2 --json /tmp/a.json >/dev/null
          python3 tools/coverage/covering.py --tool HaplotypeCaller --t 2 --json /tmp/b.json >/dev/null
          cmp /tmp/a.json /tmp/b.json && echo "identical across runs"

      - name: The measured sizing still matches the document
        # docs/what-pairwise-coverage-costs.md states these numbers. If the inventory changes, the
        # document is stale, and a stale number in a sizing document is how a plan drifts.
        run: |
          python3 tools/coverage/covering.py --all --t 2 | tee /tmp/all.txt
          grep -q "oracle runs=19437" /tmp/all.txt || {
            echo "pairwise sizing changed; update docs/what-pairwise-coverage-costs.md"
            exit 1
          }

  ci:
    name: CI complete
    # The single context main's ruleset requires, and the single context an automatic merge waits
    # on. It runs whatever the jobs above did, because a gate that is skipped when something fails
    # reports nothing and blocks nothing.
    needs:
      - guard
      - test
      - determinism
      - oracle-image
      - oracle
      - argument-coverage
      - inventory
      - dashboard
      - coverage
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Every job above succeeded
        # A skipped job is not a failure. A cancelled one is: a cancelled run has proved nothing,
        # and `success` on an empty result is how a green tick comes to mean less than it looks.
        env:
          RESULTS: ${{ toJSON(needs) }}
        run: |
          python3 - <<'PY'
          import json
          import os
          import sys

          results = json.loads(os.environ["RESULTS"])
          bad = sorted(
              name for name, job in results.items()
              if job["result"] not in ("success", "skipped")
          )
          for name in bad:
              print(f"{name}: {results[name]['result']}")
          print(f"{len(results) - len(bad)}/{len(results)} jobs green")
          sys.exit(1 if bad else 0)
          PY
