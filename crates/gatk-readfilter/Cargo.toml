[package]
name = "gatk-readfilter"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
gatk-engine = { path = "../gatk-engine" }
htsjdk-bam.workspace = true

[dev-dependencies]
# The golden is gzipped like every other corpus in the programme: they are committed, and a text
# corpus of decisions compresses to a fraction of its size.
flate2 = "1"
